## Scope guard

The frozen surface: `packages/worker/migrations/d1/0060_households_social.sql`; new Worker modules `src/social-db.ts`, `src/social.ts`, `src/member-move.ts`, `src/api/people.ts`, `src/api/join.ts` (or the equivalent split); the seam provider body in `src/visibility.ts` (NOTHING else in that module); `src/signup.ts`/`src/signup-db.ts` (fork + grammar), `src/tenant.ts`/`src/members-db.ts` (member mint, size bound, spawn), `src/session.ts` (move re-key helper), `src/admin.ts`/`src/admin/api.ts` (purge/revoke extensions, roster data), `src/tools.ts`/`src/profile-db.ts` (household export); member app People page + `/join` route + nav/badge + nickname mutation registration; admin roster regroup UI; `docs/SCHEMAS.md`, `docs/TOOLS.md`, `docs/ARCHITECTURE.md`, `AGENT_INSTRUCTIONS.md`; the seven spec files at archive. NO lens-consumer edits, NO `shared-corpus` delta, NO member columns on `overlay`/`taste`/`diet_principles`/`cooking_log`, NO new MCP tool, NO `wrangler.jsonc` change, NO new dependency or cron. Implementation MUST NOT start until `member-identity-split` and `deployment-profiles-and-visibility-lens` are merged (serial-surface rule). If implementation appears to require widening, stop and report to the main thread.

## 1. Migration and social store

- [ ] 1.1 Add `packages/worker/migrations/d1/0060_households_social.sql` per design Decision 1: `friendships` (canonical ordered pair, CHECK, index), `social_requests` (+ to/from indexes), `member_invites` (+ tenant index), `nicknames` (+ tenant index), `blocks`. Verify 0060 is still free at implementation time (0018/0045/0047 are historical duplicates — do not add another).
- [ ] 1.2 Add `src/social-db.ts` over `db(env)` (never `env.DB`), function-per-query idiom: friendship insert/delete/list-for-tenant (both orientations), request insert/resolve/list (inbox by `(to_tenant|to_member, state)`, awaiting by `from_tenant`), swallow-write, cooldown probe (latest declined `resolved_at` for a pair key), standing-cap count, invite mint/revoke/redeem/list, nickname upsert/clear/list-for-viewer, block insert/delete/match probes (tier + tenant/member), shared-count (`COUNT(recipe_imports) WHERE tenant = ?`).
- [ ] 1.3 Migration-chain tests (sqliteEnv over the full real chain, including 0058/0059): canonical-pair CHECK rejects unordered and self edges; PK dedupes both orientations at the write helper; indexes exist; all five tables purge cleanly (asserted again in §7).

## 2. Handle grammar and member minting

- [ ] 2.1 Replace `USERNAME_RE` with the shared `HANDLE_RE = /^[a-z0-9_]{3,20}$/` for every NEW mint: signup username validation in `src/signup.ts`, onboard validation in `src/admin.ts` (structured error naming the grammar), and the new member-handle chooser paths. Grandfather everything existing (no read-path validation anywhere).
- [ ] 2.2 `src/members-db.ts`: non-founding member mint (server ULID id, chosen handle, tenant, created_at) with unique-handle violation surfaced as a structured `handle_taken`; member count per tenant (size bound = 8, one shared constant beside the D24 budgets).
- [ ] 2.3 Tests: grammar acceptance/rejection matrix (underscore ok, hyphen rejected, 2 chars rejected, 21 chars rejected, uppercase rejected); grandfathered hyphenated tenant still resolves; ULID mint under a full household refused; handle collision with a founding handle (= tenant id) refused.

## 3. Requests, friendships, blocks (D24 engine)

- [ ] 3.1 `src/social.ts` operations: exact-handle lookup; request send (tier dispatch, note cap 200 inert text, display name, own-household/already-friends/duplicate-pending honest errors, swallow on cooldown or block match, standing cap 25 counting pending+declined+swallowed, self-hosted friend-tier `profile_disabled` refusal via `loadDeploymentProfile`); accept (friend → canonical edge mint; household → member mint or the §5 move flow; size bound); decline (state flip, `resolved_at`, nothing else); cancel (frees a slot, unnotified).
- [ ] 3.2 Rate limiting: `underRateLimit` keys `people:lookup:m:<member>` + `people:lookup:ip:<ip>` (30/hour) and `people:send:m:<member>` + `people:send:ip:<ip>` (10/day); structured `rate_limited` 429; fail-open (limiter-error path asserted).
- [ ] 3.3 Blocks: mint from inbox/awaiting/friend contexts (tier-scoped, `blocked_member` recorded for household tier); household-wide evaluation in the swallow probe; block-from-friend-row severs the edge in the same operation; block-from-awaiting also cancels the own outgoing request; existing pending inbound rows from the blocked party flip to `swallowed`; unblock = delete, delivers nothing retroactively.
- [ ] 3.4 Fill the friend seam: replace the empty-relation provider body in `src/visibility.ts` with the `friendships` union subquery. Assert ZERO other edits in that module (the seam contract) and run the lens change's existing consumer tests unmodified.
- [ ] 3.5 Tests: decline invisibility (requester view byte-identical before/after decline); 30-day cooldown swallow then post-cooldown delivery; cap counts swallowed rows and cancel frees a slot; one member's block binds the household (re-addressed member swallows); sever hides recipes on next lens read and edge re-accept restores; friend request visible to every target-household member and any member's accept resolves it; household invitation visible only to the invitee; swallowed notes never reach any read; self-hosted friend-tier refusal with household tier live.

## 4. Invite links and the signup fork

- [ ] 4.1 `member_invites` mint/revoke ops: >=128-bit URL-safe token, tier, 14-day expiry, single-use; friend-tier mint refused under self-hosted; cancel = revoke; uniform `invalid_or_expired` resolution for unknown/expired/revoked/redeemed.
- [ ] 4.2 `src/api/join.ts`: public `GET /api/join/:token` (inviter handle + tier; per-IP rate limit) and `POST /api/join/:token` — signed-out: household tier mints the member (handle chooser, size bound, atomic token consumption with refund-on-collision per the group-code idiom) or friend tier runs the signup path plus the edge, then mints the standard member-bound session (`createSession`) + cookie; signed-in: household tier converts to the §5 accept flow, friend tier creates the edge after confirm (idempotent). Blocked-party redemption consumes the token and creates nothing, indistinguishably.
- [ ] 4.3 `src/signup.ts`/`signup-db.ts`: extract the tenant-creation core so the friend-tier link path reuses it (registry row, founding member, allowlist, session); group codes unchanged and edge-free (regression-assert).
- [ ] 4.4 Tests: the four dead-token states are byte-identical; household redemption joins the inviter tenant with a ULID member and consumes the token atomically under concurrency (one winner); friend redemption commits tenant + edge + consumption together or not at all; signed-in conversions; foreign-kind cross-redemption refused on all three invite paths (trio separation); per-IP limiter on both join endpoints.

## 5. Member-move, leave, remove, household-accept (D23)

- [ ] 5.1 `src/member-move.ts` `moveMember` with the explicit manifest (design Decision 7): members row tenant update, `webauthn_credentials` tenant update, nicknames-set tenant update, session-scan re-key (shared D2-aware predicate with the revoke paths), bootstrap-invite deletion; refuses last-member (outside dissolution) and full destination; grants deliberately not touched (resolver pairing kills them — asserted).
- [ ] 5.2 Spawn: mint tenant id = handle when free (allowlist + registry probe) else smallest `-N` hyphen suffix; registry row + allowlist entry + founding-by-mover (id/handle unchanged); wire leave (`POST /api/people/leave`) and evict (`POST /api/people/members/:member/remove`, any-member authority, confirm client-side) to move-into-spawn; single-member floor refusal naming the alternatives.
- [ ] 5.3 Household-accept for an existing sole-member account: the confirmation gate (client renders the enumerated not-carried-over list from a server-supplied manifest — pantry, plan, list, staples, stockup, ready-to-eat, stores + store notes, Kroger link, favorites/taste/dietary/cook-history (v1), Claude re-connect); then move + grant re-key (`INSERT OR IGNORE` then delete under the `(recipe, tenant)` PK) + revoke-shaped household purge minus member rows + old-tenant retirement (allowlist, registry, requests cancelled, invites revoked, friendships severed). Multi-member requester refused with the leave-first pointer.
- [ ] 5.4 Tests: move manifest (moved vs untouched tables, byte-level); mover's session survives re-keyed while a raced stale record 401s; mover's MCP grant stops resolving; spawn id collision ladder (`bob` → `bob-2`); dissolution end-to-end (grants re-keyed with duplicate collapse, household rows purged, member rows survive in the absorber, `tenant:` allowlist gone, awaiting/invite rows terminally resolved); leaver takes zero `recipe_imports`; floor and size-bound refusals.

## 6. People API and page

- [ ] 6.1 `src/api/people.ts` + mount in `src/api/app.ts`: `GET /api/people` aggregate (members + viewer nicknames, friends + shared counts, inbox, awaiting = outgoing requests + unredeemed invites; friend sections omitted under self-hosted), lookup, requests CRUD (send/accept/decline/cancel), blocks (create/delete), unfriend, nickname PUT (upsert/empty-clear, others-only, target-exists validation), invites (mint/revoke), leave/remove. All session-gated; write classifications per member-app-offline (nickname is the only class (b) write).
- [ ] 6.2 People page (`packages/app/src/routes/_app.people.tsx` + NAV entry + `lib/data.ts` hooks + `lib/api.ts` types): SaaS full variant per the member-app-core delta — inbox (@handle ALWAYS, tier badge, inert quoted note, relative time, accept/decline/block), nickname hint + live example from real nicknames, HOUSEHOLD (avatar local-color popover in localStorage only, name states, inline nickname edit, remove-with-confirm), split-button adders, awaiting with cancel/block, FRIENDS ("N shared" chips, unfriend confirm, empty state). Accept flows: nickname seed moment; existing-account movers get the not-carried-over confirmation.
- [ ] 6.3 Self-hosted variant (design request #12, authorized local design — mimic the export's styles and primitives; the design-requests prompt is the brief): same component, alternate state gated on whoami `profile`; FRIENDS + tier badges + friend copy absent; rewritten header; nickname hint side-by-side on wide viewports.
- [ ] 6.4 `/join/:token` route (`packages/app/src/routes/join.$token.tsx`): tiered landing, signed-out account form (handle/username chooser, optional display name), signed-in conversion, uniform dead-token state. Confirm NO `run_worker_first` entry is added (SPA fallback) and the SW denylist is untouched.
- [ ] 6.5 Sidebar badge: `useSidebarCounts` gains `people` from the same people aggregate query the page uses; zero renders no badge; register the nickname mutation key in the class (b) registry; the people read stays OFF the persistence allowlist.
- [ ] 6.6 Playwright (`aubr test:app`) through the real seeded API: inbox accept/decline (requester view unchanged after decline), nickname edit + empty-clear + hint example, remove confirm, invite mint/copy/cancel, awaiting rows, both profile variants, join landing (valid + dead token), badge count.

## 7. Operator admin

- [ ] 7.1 `src/admin.ts`: household-purge extensions (both-direction social cleanup helper: friendships, requests, invites, nicknames incl. rows targeting its members, blocks both directions); member-revoke extensions (nicknames set/targeting, outgoing requests cancelled, minted invites revoked, `blocked_member` records); one shared cleanup helper for both scopes; `TENANT_TABLES` gains the tenant-keyed social tables where applicable.
- [ ] 7.1b Multi-member purge/liveness follow-throughs (identity-split review findings, load-bearing the moment member #2 exists): (a) household-purge's AUTHOR_TABLES delete switches from `author = <tenant id>` to the member-set subquery (`author IN (SELECT id FROM members WHERE tenant = ?1)`) ORDERED BEFORE the `members` delete in the batch — otherwise non-founding members' notes orphan (fix the admin.ts comment + SCHEMAS.md purge line back to the plural wording when done); (b) gate both `/authorize` grant-completion sites (legacy-invite POST and cross-device status claim) through `resolveIdentity` so a revoked member's lingering invite/approval can't mint even a dead grant (parity with `/api` login); (c) guard `revokeMember`'s count-then-delete against the concurrent last-two-members race (conditional delete or re-check in the batch) — a zero-member allowlisted tenant must stay unrepresentable. Tests for all three.
- [ ] 7.2 Roster regroup (`src/admin/api.ts` data + `packages/admin-app/src` screens/queries): household groups with member rows, Households stat tile, split household-level vs member-level action menus, compact single-member rendering, last-member menu routes to purge; member-revoke surfaced.
- [ ] 7.3 `admin/visual/` Playwright: regrouped roster (single- and multi-member households), member-revoke flow, last-member routing, purge-clears-social assertion via seeded data; run `aubr test:admin`.

## 8. Tool, persona, and docs lockstep (Appendix B/C band 5, this change's slice)

- [ ] 8.1 `read_user_profile` (`src/tools.ts` `assembleUserProfile`, also `GET /api/profile`): add the `household` block `{ members: [{ handle, nickname, you, joined_at }] }` — caller's own nicknames only, null when unset; never self-nicknames or others' aliases.
- [ ] 8.2 `docs/TOOLS.md`: the `read_user_profile` section documents the household block and its guarantees (per-viewer nickname privacy; the tool never returns nicknames set by others or for the caller). No other tool contract changes.
- [ ] 8.3 `docs/SCHEMAS.md`: sections for `friendships`, `social_requests`, `member_invites`, `nicknames`, `blocks`; the invite-kind trio distinction beside the existing invite/group-code sections; the members-section note on new-mint grammar + ULID ids + spawned-tenant founding. Current-state language only.
- [ ] 8.4 `docs/ARCHITECTURE.md`: extend the multi-tenant identity section with household membership (any-member governance, size bound), the friendship relation feeding the lens seam, and member-move (incremental — the wholesale rewrite belonged to the lens change).
- [ ] 8.5 `AGENT_INSTRUCTIONS.md`: the session-start profile read carries household members + nicknames (resolve "Mom and Grandma" style references from the profile export; handles are the stable keys). Run `aubr build:plugin --check`.

## 9. Verification and handoff

- [ ] 9.1 Production fixture capture (GATED on operator permission; read-only; pre-merge): `SELECT COUNT(*) FROM tenants;` and `SELECT id FROM tenants WHERE id NOT GLOB '[a-z0-9_]*' OR length(id) < 3 OR length(id) > 20 OR id GLOB '*-*';` (the grandfather inventory — expected non-empty, recorded, never migrated); `SELECT COUNT(*) FROM members WHERE id != tenant;` (expect 0 pre-deploy); `SELECT tenant, COUNT(*) FROM recipe_imports GROUP BY tenant;` (post-0059 shared-count sanity); a `session:*` sample confirming the re-key predicate's record shapes. Encode observed divergences as test fixtures before merging.
- [ ] 9.2 Run `aubr typecheck`, `aubr test` (worker: §1–§5 + §7 suites plus untouched suites green — lens/visibility tests must pass with zero edits), `aubr test:app`, `aubr test:admin`.
- [ ] 9.3 Run `openspec validate households-friends-and-people-page --strict`, the plugin `--check` from 8.5, and `git diff --check`; sync the approved deltas into the living specs at archive time.
- [ ] 9.4 Report to the main thread: the exact diff footprint, the D23 v1 move-manifest reduction (the named follow-on that member-keys overlay/taste/diet/cook-log and extends the manifest), the mock inbox-copy correction ("invites you to join their household"), the grammar-tightening operator impact, and any grandfather-inventory surprises from 9.1.
