# Tasks — meal-dimension-foundations

**Sequencing (band-serial):** this change implements **last in band 1**, after `brand-tier-model` (which introduces the `warnings` return field + the TOOLS.md deprecation convention this change extends), `pantry-disposition-foundations` (shares the `scheduled()` wiring — its `ingredient-category` job lands first), and `spend-capture-on-order-commit` (shares the `update_preferences`/`read_user_profile` TOOLS/SCHEMAS sections). Do not implement in parallel with them. Downstream serial surfaces: `meal-plan-widget`/`member-app-propose` shared with band-2 `plan-your-week-widget`; `meal-planning` shared with band-2 `meal-plan-page`. Each numbered section below leaves the tree green (`aubr typecheck` + `aubr test`).

## 1. Pure logic (no D1, no wiring)

- [x] 1.1 Add `packages/worker/src/ids.ts`: ~20-line pure `ulid()` over `crypto.getRandomValues` (workerd-safe, no dependency) + the row-id regex `^[0-9A-Za-z_-]{10,40}$`. Unit-test mint shape and regex acceptance of both ULIDs and 32-hex migration ids.
- [x] 1.2 Rewrite `meal-plan.ts` op resolution to id-keyed rows: the `add` resolution order (id replay/update with recipe-mismatch guard → `duplicate: true` insert → slug-global case-insensitive coalesce with 0/1/>1 branches and `candidates` conflict), `remove` split idempotency (id → `removed: 0|1`, slug → fan-out with removed ids, zero-match conflict), `set` (id any-field incl. `recipe`/`meal`; slug unique-or-candidates, no recipe change), project-row constraint conflicts (`"project rows carry no date or sides"`), the shared earliest-due selector (`planned_for ASC NULLS LAST, id ASC`), `coalesced: true` + survivor-id reporting, `last_planned_at` stamp on any applied add.
- [x] 1.3 `preferences.ts`: `RETIRED_PREFERENCE_KEYS` checked before `rejectUnknownPatchKeys` (accept-and-drop, never `validation_failed`, never the nest-under-`custom` hint, never written to `custom`); defined `cadence` key with per-key RFC 7396 merge (per-key set/clear, `cadence: null` clears the map, ints 0–7); `default_cooking_nights: N` alias merged as `cadence.dinner = N` (never touching the frozen column); `warnings: [{ key, reason, superseded_by }]` accumulation, present only when non-empty (extends the `brand-tier-model` convention — verify its shape landed and match it exactly).
- [x] 1.4 Attendance/blend pure functions: `householdRoster(env, tenant) → [tenant]` (the one band-5 seam), `unionHardConstraints(profiles)`, `blendTasteProfiles(profiles, eating)` (uniform weights; singleton = identity blend, today's ranking byte-for-byte), attendance resolution (`exactly one of away/only` → `validation_failed`; unknown handles dropped and echoed; `only∩roster` / `roster−away`; empty effective set fails open to the full roster + note), vibe participation filter (`members ∩ effective ≠ ∅`; NULL = everyone; all-unresolvable members fail open to everyone + note). Unit-test with synthetic multi-profile fixtures now.
- [x] 1.5 Unit tests over the full ops-resolution matrix (every add/remove/set branch, replay idempotency in every branch, cross-meal coalesce = move, >1-match candidates) + F1–F5-shaped fixture data.

## 2. Migration

- [x] 2.1 Add `migrations/d1/NNNN_meal_dimension.sql` (design §1 verbatim; `NNNN` = next free number after the band-1 siblings' migrations): `meal_plan` PK rebuild with SQL-minted 32-hex ids + `meal` column + `(tenant, recipe)` index; nullable `cooking_log.meal`; `night_vibes.meal` (NOT NULL default `'dinner'`) + `members`; `profile.cadence` backfill from non-NULL `default_cooking_nights`. No unique recipe index, no column drops, no project CHECK, no `night_vibe_derived` change.
- [x] 2.2 Local-D1 migration test: seed the spike pre-states verbatim (design §1 fixtures table), apply the migration, assert F1 (3 rows, distinct `^[0-9a-f]{32}$` ids, `meal='dinner'`, `sides` byte-identical, `planned_for`/`from_vibe` NULL), F2 (7 vibes `meal='dinner'`, `members` NULL, `night_vibe_derived` untouched — no re-embed), F3 (cadence backfill incl. column-wins-over-`custom` precedence, `everett` NULL, absent rows untouched, `custom` byte-identical), F4 (4 log rows `meal` NULL).

## 3. D1 plumbing

- [x] 3.1 `session-db.ts`: re-key meal-plan statements on `(tenant, id)` — upsert-by-id, delete-by-id, delete-by-slug fan-out, slug lookup (case-insensitive, cross-meal), the earliest-due selector; drop the `ON CONFLICT(tenant, recipe)` upsert.
- [x] 3.2 `cooking-write.ts`: the deterministic clear order (`plan_row_id` → exact `(recipe, meal, date)` → earliest-due excluding `meal='project'` unless `entry.meal='project'` → no clear), at most ONE row cleared, `from_vibe` read from the row actually cleared, delete by `(tenant, id)` in the same atomic batch, `cleared_plan_row` in the result, stale-`plan_row_id` = no clear + log still written + note (no fall-through), recipe-mismatch `conflict` with no log written; route dedupe identity `(date, meal, type, recipe|name)` (NULL meal matches NULL only); meal-scoped vibe attribution (entry meal set → candidate vibes of that meal; NULL → all vibes; `from_vibe` prior always resets).
- [x] 3.3 `night-vibe-db.ts`: `meal` + `members` on read/insert/update; explicit-null clearing for `cadence_days`/`base_weight`/`weather_affinity`/`weather_antipathy`/`season`/`facets`/`members`; `meal` settable, not nullable; `members` validated non-empty strings, deduped, stored verbatim.

## 4. Tools

- [x] 4.1 `tools.ts`: register `list_meal_vibes`/`add_meal_vibe`/`update_meal_vibe`/`remove_meal_vibe`/`suggest_meal_vibes` as canonical; keep `*_night_vibe` + `suggest_night_vibes` registered as dispatch aliases onto the identical shared ops (identical requests/responses, no warnings injection), descriptions replaced with the one-line "Deprecated alias of `<new name>` — identical behavior; use the new name."
- [x] 4.2 `update_meal_plan`/`read_meal_plan`: wire §2.1/§2.2 (id-regex validation; flat meal-ordered read — dated by `(planned_for, breakfast<lunch<dinner)`, undated grouped by meal, projects last, `id ASC` tiebreak; `id` documented in the tool description as the row address + replay key).
- [x] 4.3 `log_cooked`: `meal` + `plan_row_id` params; additive `cleared_plan_row` return; tool description carries the clear order, the one-row guarantee, and the stale-id no-fall-through semantics (tool-vs-skill boundary).
- [x] 4.4 `update_preferences`/`read_user_profile`: wire the shim/cadence/warnings from 1.3; profile export gains `cadence` (stored map, else `{breakfast:0, lunch:0, dinner: default_cooking_nights ?? 5}`), keeps `default_cooking_nights` as a derived mirror of `cadence.dinner` for the window, drops `lunch_strategy`/`ready_to_eat_default_action` from the export now; palette section under meal-vibe naming with per-vibe `meal` (+ `members` when set); `missing` label stays `"vibes"`. (Budget / brand-tier / household-member export shapes are owned by sibling/band changes — out of scope here, deliberately.)
- [x] 4.5 `retrospective`: meal-aware cadence — overall `cooks_per_week` unchanged, `by_meal: {breakfast, lunch, dinner, project}` over meal-set rows, `meal_unknown: N` for NULL rows. (Spend/waste aggregates: band 4 / the spend sibling — not here.)

## 5. Propose engine

- [x] 5.1 `meals` map param (per-window counts 0–14; default chain explicit → stored `cadence[meal]` → derivation) + `nights` alias (= `meals.dinner`, ignored when `meals` supplied, no error); per-meal palette partition + per-meal `sampleWeek` sampling; weather quotas in the dinner pass only (breakfast/lunch see a neutral all-`mild` histogram; stored non-dinner affinities inert); string `lock`s and `new_for_me` force-placements are dinner slots.
- [x] 5.2 Vibe-meal binding: count > 0 with zero vibes of that meal → explicit empty slots (`empty_reason: "no_palette_for_meal"`) + a `notes[]` entry naming the escapes; never a cross-palette fallback.
- [x] 5.3 Meal-aware course gate: dinner/lunch keep the `main`-or-empty fail-open gate; breakfast gates on `course` includes `breakfast` or empty (fail-open); explicit `facets.course` overrides; locks/pins exempt.
- [x] 5.4 One shared fill/compose pass across all meals (`buildPool` per slot, one `assembleProposal`): cross-slot MMR/diversify, facet-spread, at-risk set-cover, and the engine-side no-duplicates invariant (one recipe at most once per proposal; explicit pins/locks exempt). Still at most one batched embedding call per request. `ephemeral_vibes[].meal` (default `dinner`) authors slots with meals.
- [x] 5.5 Attendance: wire 1.4 into the shared op (`propose_meal_plan`, `display_meal_plan`, `POST /api/propose` — identical); `diagnostics.attendance = { effective, ignored }`, `diagnostics.meals = { <meal>: { requested, filled, empty } }`, `diagnostics.nights` kept as the dinner alias.
- [x] 5.6 `@yamp/contract` `ProposeCardData`: slots gain `meal` (flat, meal-ordered breakfast → lunch → dinner, position-stable within meal); `request` carries `meals` + `attendance`. Data shape only — NO widget/UI work.
- [x] 5.7 Commit path (shared op, no UI): each filled slot → an `add` op with a client-minted ULID, the slot's `meal`, and its `from_vibe`; `duplicate` never set; a slug the member explicitly duplicated surfaces the candidates conflict.

## 6. Cron + routes (serial: shared `scheduled()` wiring)

- [x] 6.1 `nameCluster` third reply line (vibe phrase / weather-bucket label / meal label); `parseMealLabel` mirroring `parseBucketLabel`, strict closed-set match, fail-closed to `'dinner'`; discard the weather-bucket label for non-dinner classifications; `starterVibesFromTaste` → `meal: 'dinner'`; suggestions carry `meal` through the pending-proposal payload.
- [x] 6.2 `night-vibe-dedupe.ts`: phrase-space convergence key → `(meal, phrase-space)`; pending proposals lacking `meal` treated as `dinner`.
- [x] 6.3 New `runPrefRetirementSeedJob`, `scheduled()` phase 5 (register after `pantry-disposition-foundations`' job — coordinate the wiring): for each profile row with `lunch_strategy IS NOT NULL OR ready_to_eat_default_action IS NOT NULL`, enqueue the design-§4.2 mapping as `add_vibe` proposals (deterministic targets `pref-retire:lunch_strategy` / `pref-retire:rte`; `(tenant, kind, target)` enqueue idempotency; `opt-in` seeds nothing) **and NULL both retired columns in the same D1 batch**. Never read or write `custom`. Job health recorded like the other background jobs.
- [x] 6.4 `src/api/vibes.ts` `POST /vibes/suggest` → the pinned 410 stub: `c.json({ error: "gone" as const, message: "Vibe suggestions now arrive automatically; this trigger was retired." }, 410)` (member-API route-level convention, NOT a `ToolError` code). Update `test/api-member.test.ts` and the app suite's throttled-suggest coverage to assert the 410 stub (the shipped button now fails explicably; band 2 removes the button; the window-close cleanup removes the stub).
- [x] 6.5 Unit tests: three-line parse incl. fail-closed + bucket discard; `(meal, phrase)` dedupe; seed job — F5 shape (two proposals for casey, both columns NULL, second tick no-op, `custom` byte-identical), enqueue-idempotency crash-window coverage, absent-profile-row skip.

## 7. Docs, specs, and persona lockstep (same PR)

- [x] 7.1 `docs/TOOLS.md`: every design-§2 delta; extend the **Deprecations** section (introduced by `brand-tier-model`) with rows — 4 vibe aliases + `suggest_night_vibes`, `propose.nights` + `diagnostics.nights`, the `default_cooking_nights` write alias, the retired-key accept-and-drop, the `read_user_profile.default_cooking_nights` mirror, the `/api/vibes/suggest` 410 stub — each with the design-§9 removal condition; extend the `warnings` convention paragraph. Audit the tool-vs-skill boundary: fan-out, `duplicate: true`, clear order, warnings, attendance fail-open, `empty_reason` all in tool descriptions.
- [x] 7.2 `docs/SCHEMAS.md`: `meal_plan` new shape (id contract + arbitrary-but-deterministic tiebreak, project-row constraints, ordering); `cooking_log.meal` + the dedupe-identity clarification; `night_vibes.meal`/`members` ("meal vibes — stored in the `night_vibes` table"); preferences `cadence` (per-key merge) + retired-keys window note; `ProposeCardData`. Current-truth phrasing only.
- [x] 7.3 `docs/ARCHITECTURE.md`: rewrite the class (b) keying sentence — **both clauses** ("plan rows by client-minted row id (ULID; slug-addressed ops keep defined fan-out…)" AND "log rows deduped on `(date, meal, type, recipe|name)`"); menu-generation paragraph gains per-meal counts, per-meal palettes, attendance input; delete the `POST /api/vibes/suggest` health-gate sentence.
- [x] 7.4 Persona (`packages/worker/AGENT_INSTRUCTIONS.md`): (a) meal-plan skill — per-meal counts from `cadence`, vibe-meal binding, empty-meal nudge, thread `meal` + adopt returned row ids on commit, never `duplicate: true` unless the member asked, conversational attendance ("the kids are gone this weekend" → `attendance: { away: [...] }`; "just the two of us" → `only`); (b) onboarding "Cooking rhythm" — capture the per-meal `cadence` map, then offer `suggest_meal_vibes` to seed lunch/breakfast vibes when those cadences are nonzero (lunch-strategy/RTE-default questions: grep-verified absent from persona prose — nothing to remove, replacement added; the "Heat-and-eat acceptance" question is untouched); (c) cooked/log flow — pass `meal` (infer from context/time of day, ask when ambiguous, omit for non-meal events, never guess `project`), mention `plan_row_id` when anchored on a specific slot/duplicate; (d) terminology "night vibe(s)" → "meal vibe(s)" persona-wide, new tool names only; (e) retrospective prose off `by_meal`. (Budget capture belongs to the spend sibling — not edited here.)
- [x] 7.5 Gates: `aubr build:plugin --check` AND the Appendix-C grep gate — `grep -E 'lunch_strategy|ready_to_eat_default_action|night.vibe' packages/worker/AGENT_INSTRUCTIONS.md` comes back clean before merge.
- [x] 7.6 Product-specs extras: strike through stories/02 Q2–Q4 with the design-§8 resolutions (Q1-style); edit `product-specs/CHANGES.md` — move the D8 pref-retirement clause from `profile-planning-and-vibes-ui` to this change's entry (D25(2)'s UI coupling stays band 2's).
- [x] 7.7 `aubr typecheck` + `aubr test` green; `openspec validate "meal-dimension-foundations" --strict` passes.

## 8. Post-merge / post-deploy verification (read-only against production)

- [ ] 8.1 Single deploy (migration `--remote` → Worker → plugin publish). Include the `wrangler.jsonc` `database_name: "yamp"` vs production `grocery-mcp` mismatch flag in the PR description for the deploy-repo owner (out of scope to fix here; binding is by `database_id`).
- [ ] 8.2 F1–F4 `--remote` acceptance queries (design §1 table, read-only).
- [ ] 8.3 F5 after the first cron tick (two `pref-retire:*` proposals for casey, both retired columns NULL, other tenants untouched, `custom` byte-identical); re-check after the second tick (no change — idempotency/termination). Optional probe: if casey accepts a seed, confirm the next vector-reconcile tick embeds it.
- [ ] 8.4 At archive time, create the follow-up change `remove-meal-dimension-shims` (window close: a subsequent plugin publish P₂ AND ≥30 days since P₁; table-driven off TOOLS.md's Deprecations section; includes the `00NN` column-drop migration gated on the retired columns being NULL everywhere).
