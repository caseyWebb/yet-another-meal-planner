# Tasks

## 1. Food guard + funnel wiring

- [x] 1.1 Add `isFoodItem(kind, domain)` (`src/grocery.ts`): `true` iff `kind === "grocery"` and `domain` is grocery/absent, plus `groceryKey(name, kind, domain, resolve)` = `isFoodItem ? resolve(name) : normalizeName(name)`. Unit-tested (`test/grocery.test.ts`): `kind: grocery, domain: pharmacy` → false, household/other → false, defaults → true.
- [x] 1.2 Threaded an optional `resolve` (default `normalizeName`) into the pure grocery ops (`findIndex` — now a dual-key matcher, `addToGroceryList` / `updateGroceryItem` / `removeGroceryItem`) and `computeToBuy`. `addToGroceryList` gates the resolver by `isFoodItem` so a non-food add never touches the capturing funnel. Ops stay pure (inject the resolver, no env).
- [x] 1.3 `src/session-db.ts`: `pantryUpsertStmt`/`groceryUpsertStmt` take `resolve` and bind `resolve(name)` / `groceryKey(...)` as `normalized_name`; `addGroceryRow`/`updateGroceryRow`/`removeGroceryRow`/`advanceInCartRows`/`applyPantryRowOps`/`markPantryVerifiedRows` build `ingredientContext(env).catch(() => emptyIngredientContext(env))` and thread `ctx.resolve` into the pure op + the byName/byKey maps + the upsert. Capture rides `resolve` (food); non-food grocery uses `normalizeName` (never captures).

## 2. Matching + set math

- [x] 2.1 `src/pantry-write.ts` `matches(item, name, normalize)` compares `normalize(...)` on both sides; `applyPantryOperations`/`markVerified` gained a trailing `normalize` (default `.toLowerCase()`), production passes `ctx.resolve`. Pantry is food by construction, so it funnels wholesale — canonical-id (lowercased, so the case-insensitive guarantee holds).
- [x] 2.2 `src/order.ts` `computeToBuy` gained `resolve?` (default `normalizeName`); list dedup via `groceryKey`, menu-need merge via `resolve`, pantry ⊖ join on the canonical id (`pantryNames` already holds canonical ids), so an on-hand item cancels its grocery/menu counterpart across surface forms. `placeOrder` gained `resolveKey?` so the override lookup keys by the same funnel the map is built with (`order-tools.ts` keys `quantities`/`includePartials`/`overrides` by `ctx.resolve`).
- [x] 2.3 `removeGroceryRow` deletes BOTH candidate keys (`normalized_name IN (resolve(name), normalizeName(name))`, deduped when equal) so a case/quantity/alias-varying removal hits its row (the dual-key `findIndex` handles the in-memory found-check).

## 3. Migration / reconcile (decision D2)

- [x] 3.1 `src/grocery-pantry-reconcile.ts` — `reconcileGroceryPantryKeys(env)` re-keys stale food `pantry`/`grocery_list.normalized_name` from `normalizeName` to `resolve` across all tenants, via a pure `planReconcile(rows, targetKeyOf, merge)` (groups by tenant+target; a group needing re-key = a collision or a stale singleton; DELETE non-target PKs + one merged UPSERT, kept together so the per-tick bound never splits a re-key). Collision-merge rules per the design (grocery: union `for_recipes`, real-over-"1" `quantity`, earliest `added_at`, most-advanced `status`, first non-null `note`; pantry: earliest `added_at`, freshest `last_verified_at`, latest `quantity`, first non-null `category`/`prepared_from`/`notes`). Reuses the exported `pantry/groceryUpsertStmt` so the new key is computed identically to the write path. Idempotent; leaves `name` untouched. Wired into the `scheduled()` Phase 1 (independent of the recipe pipeline; a no-op once canonical, so it self-terminates). Bounded to `RECONCILE_MAX_PER_TICK = 500` re-key groups/tick, logging on truncation (no silent cap).
- [x] 3.2 `test/grocery-pantry-reconcile.test.ts` — pure planner (skip-canonical, stale-singleton delete+upsert, collision grouping, canonical-PK-not-deleted) + driver end-to-end (re-key grocery, real collision merge preserving `for_recipes`/`status`/`added_at`, non-food left on `normalizeName`, idempotent second pass, pantry re-key).
- [x] 3.3 **Decided: reconcile-backfill** (Open Question #1) — a bounded, idempotent scheduled pass, the house reconcile pattern; it self-terminates once every row is canonical. Lazy-re-key was NOT adopted (the dual-key lookups already tolerate old-keyed rows for reads/removes, so the backfill alone closes the merge-add duplicate window). No dual-read transition needed.

## 4. Docs + guarantees

- [x] 4.1 `docs/SCHEMAS.md`: `pantry` (always canonical id via the funnel — food by construction) + `grocery_list` (`isFoodItem` guard: canonical id for food, `normalizeName` for non-food, never resolved/captured) `normalized_name` semantics, in both the prose and the SQL column comments.
- [x] 4.2 `docs/ARCHITECTURE.md`: the fragmentation-store paragraph — all four stores now key on the canonical id through the funnel, with the non-food carve-out and pantry-funnels-wholesale stated inline.
- [x] 4.3 **Confirmed** (Open Question #3): the capture cron drains a bounded FIFO (`NORMALIZE_MAX_PER_TICK = 25` via `readNovelTermsBatch`), and `novel_ingredient_terms` upserts by term (deduped). A burst of food grocery/pantry captures just queues and drains over ticks — the existing per-tick bound absorbs it with no unbounded growth; no dedicated drain budget added.
