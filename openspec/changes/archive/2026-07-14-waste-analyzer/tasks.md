## Traceability and scope guard

Requirement keys used below are `WT1` MCP/profile aggregate, `WT2` bounded UTC windows, `WT3` versioned avoidability, `WT4` spend-history-only value, `WT5` exact shared shape, `WT6` coverage/weeks/count/trend, `WT7` Waste rate, `WT8` breakdowns, `WT9` most-wasted items, `WT10` insight, `WT11` read-only projection, `WT12` production contracts/tests, `MA1` member shell, `MA2` member endpoint, `MA3` Waste panel, and `MA4` production-entry browser/API proof. A reference to a requirement includes its complete scenario cluster; the tasks listed for that requirement collectively accept every scenario. Each task's `(maps: ...)` clause is the task-to-requirement matrix.

The frozen implementation surface is:

- `packages/worker/src/spend.ts`, limited to exporting the existing pure `SpendBounds`, `spendBounds`, `addUtcDays`, `toCents`, `fromCents`, `roundPercent`, `compareRawKeys`, and `presentationLabel` declarations without changing their bodies, call sites, or Spend behavior;
- `packages/worker/src/waste-shapes.ts`, exported from `packages/worker/package.json` as the workerd-free production wire contract shared by Worker, app, and typed presentation fixtures;
- `packages/worker/src/waste-avoidability.ts`, `packages/worker/src/waste-analyzer.ts`, `packages/worker/src/api/retrospective.ts`, and `packages/worker/src/cooking-tools.ts`;
- focused production-entry coverage in `packages/worker/test/waste-analyzer.test.ts`, `api-member.test.ts`, and `cooking-tools.test.ts`;
- `packages/app/src/lib/data.ts`, `packages/app/src/routes/_app.retrospective.tsx`, and existing shared rules in `packages/ui/src/cookbook.css`;
- shared real-API fixtures in `packages/worker/admin/visual/seed.mjs` and `seed.d.mts`, plus `packages/worker/app/visual/pages/retrospective.page.ts` and `specs/retrospective.spec.ts`;
- the two living specifications, `docs/{TOOLS,SCHEMAS,ARCHITECTURE}.md`, and `packages/worker/AGENT_INSTRUCTIONS.md`; and
- the active OpenSpec artifacts that the separate archive step will move after implementation.

This is the exact anticipated 29-file set described by D13, inside the frozen 24-32 file / 4,200-5,600 addition envelope. `packages/worker/package.json` is the one anticipated authored manifest/configuration edit. No migration, index, dependency, cron, generated bundle, `packages/app/src/styles.css`, parallel API area/model, synthetic mapping version, or unrelated surface is anticipated.

| Product/proposal cluster | Requirement/design source | Implemented and accepted by tasks |
|---|---|---|
| Household authorization, UTC ISO weeks, 4/8/12 selected/prior bounds, future exclusion, and bounded indexed reads | WT2, WT11; D1, D10 | 1.1, 1.4, 2.1, 2.6, 3.1-3.4 |
| Spend-history-only last-paid value, missing/estimated/zero/void/tie behavior, quantity ignored, and no fallback | WT4, WT6; D3, D5 | 1.4-1.6, 2.2, 2.4, 4.3-4.4, 6.3 |
| Immutable reason-only v1 mapping, exact five/five classes, named replay, and explicit unknown-version failure | WT3; D2 | 1.3, 2.3, 3.1-3.4, 6.2-6.4 |
| Effective Leftovers, pending department, exact coverage, and department/reason/avoidability denominators | WT5, WT8; D4, D7 | 1.2, 1.5, 1.7, 2.4-2.5, 4.3-4.4 |
| Tossed value, exact Items binned/per-week, weekly buckets, trend, and zero/missing denominator behavior | WT5-WT6; D4-D5 | 1.2, 1.5-1.6, 2.4-2.5, 4.3-4.4 |
| Waste rate with Household excluded, Beverages included, exact input gating, reason precedence, and available-only 10% treatment | WT7; D6 | 1.6, 2.4-2.5, 4.4-4.5, 5.3-5.4 |
| Stable breakdown/item ordering, sparse groups, representatives, cap, and deterministic insight templates | WT8-WT10; D7-D9 | 1.7-1.8, 2.5, 4.4, 5.3 |
| Additive API, MCP, and profile defaults/selectors/shared object/ETag with no Waste writer | WT1, MA2; D11 | 1.2, 3.1-3.4, 6.2 |
| Accessible shared URL controls and truthful loading/empty/error/unavailable/partial/complete responsive Waste UI | MA1, MA3-MA4; D12-D13 | 4.1-5.4, 7.1-7.2 |
| Late, duplicate, void-corrected, concurrent, repeated, fresh/current-schema, deterministic read-only behavior | WT11-WT12; D10, D13 | 1.1, 1.4, 2.6, 3.2, 7.1, 7.4 |
| Exact living-spec/tool/schema/architecture/persona/plugin alignment and no analyzer cron | WT12; D13 | 6.1-6.5, 7.3 |
| Explicit non-goals, simplest existing architecture, and forecast/hard-stop controls | Proposal non-goals/impact; D10, D13 | 1.1, 7.4 |

| Spec requirement | Implemented and accepted by tasks |
|---|---|
| WT1 MCP/profile aggregate | 1.2, 3.3-3.4, 6.2, 7.1 |
| WT2 bounded UTC windows | 1.4, 2.1, 3.1-3.4, 6.4 |
| WT3 versioned avoidability | 1.3, 2.3, 3.1-3.4, 6.2-6.3 |
| WT4 spend-history-only value | 1.4-1.5, 2.2, 6.3 |
| WT5 exact shared shape | 1.2, 1.5-1.8, 2.4-2.5, 3.1-3.4, 4.1, 6.2 |
| WT6 coverage/weeks/count/trend | 1.5-1.6, 2.4-2.5, 4.3-4.4 |
| WT7 Waste rate | 1.6, 2.4-2.5, 4.4-4.5, 5.3-5.4 |
| WT8 breakdowns | 1.5, 1.7, 2.4-2.5, 4.4, 5.3 |
| WT9 most-wasted items | 1.7, 2.5, 4.4, 5.3 |
| WT10 insight | 1.8, 2.5, 4.4, 5.3 |
| WT11 read-only projection | 1.1, 1.4-1.5, 2.6, 3.2, 7.4 |
| WT12 production contracts/tests | 1.1-7.4 |
| MA1 member shell | 4.1-4.2, 4.5, 5.2-5.4, 7.2 |
| MA2 member endpoint | 3.1-3.2, 4.1, 6.2, 7.1 |
| MA3 Waste panel | 4.1-4.5, 5.2-5.4, 7.2 |
| MA4 production-entry browser/API proof | 2.1-3.4, 5.1-5.4, 7.1-7.2 |

| Design decision | Implemented and accepted by tasks |
|---|---|
| D1 household/clock/ranges/auth | 1.4, 2.1, 3.1-3.4 |
| D2 immutable named mappings | 1.3, 2.3, 3.1-3.4 |
| D3 indexed last-paid value | 1.4-1.5, 2.2 |
| D4 exact aggregate contract | 1.2, 1.5-1.8, 3.1-4.4 |
| D5 coverage/weeks/count/trend | 1.5-1.6, 2.4-2.5 |
| D6 qualifying Spend/Waste rate | 1.6, 2.4-2.5, 4.4-4.5 |
| D7 effective department/denominators | 1.5, 1.7, 2.4-2.5, 4.4 |
| D8 most-wasted ordering | 1.7, 2.5, 4.4 |
| D9 deterministic insights | 1.8, 2.5, 4.4 |
| D10 indexed/read-only/concurrency | 1.1, 1.4-1.5, 2.6 |
| D11 API/MCP/profile/compatibility | 3.1-3.4, 6.2 |
| D12 member UI/accessibility | 4.1-5.4 |
| D13 production tests/docs/footprint | 2.1-7.4 |

## 1. Shared bounded Waste reader

- [x] 1.1 Before production edits, record the merge-base footprint and enforce the ratified surface/non-goals. If Waste requires any capture/writer change, migration/index/cron/dependency, value fallback, synthetic state, cart recovery/ownership, pantry or grocery token, generic CAS/error framework, re-key convergence, settlement/compensation, operation registry/oracle, or satellite receipt atomicity, document the exact production-reachable blocker and stop for user approval without editing that system. (maps: WT11 all bounded/read-only scenarios; WT12 production-design/compatibility scenarios)
- [x] 1.2 Add the exact workerd-free production wire types and literal unions in `packages/worker/src/waste-shapes.ts`, import/reuse the public workerd-free Spend range/coverage/money primitives, export the Waste subpath from `packages/worker/package.json`, and use that one contract from Worker, app, and typed presentation fixtures. Acceptance: fields/statuses/reasons/cap match D4 exactly, including nonempty most-wasted groups using only `unavailable | partial | complete`; this is production contract code, not a test-only abstraction, and no copied Spend primitive or transport/UI response model survives. (maps: WT1 shared-object scenarios; WT5 every shape/arithmetic/orthogonality scenario; WT9 nonempty-group-status scenario; WT12 contract-alignment scenarios; MA2 direct-shape scenarios; MA4 typed-interception scenario)
- [x] 1.3 Implement `packages/worker/src/waste-avoidability.ts` as the exhaustively typed, immutable `waste-avoidability-v1` five/five reason-only registry with current-version metadata and the exact unsupported-version `ToolError`. Do not add a synthetic v2, effective-date branch, future-only current state, member override, or non-reason heuristic. (maps: WT3 every mapping, default, named-replay, validation, and ignored-field scenario; WT11 no-synthetic-state scenario)
- [x] 1.4 In `packages/worker/src/spend.ts`, export only the existing pure `SpendBounds`, `spendBounds`, `addUtcDays`, `toCents`, `fromCents`, `roundPercent`, `compareRawKeys`, and `presentationLabel` declarations with no body, caller, or behavior change. Import and reuse all eight from `packages/worker/src/waste-analyzer.ts`; do not create a generic helper module or copied helper. Implement `readWasteAnalyzer(env, tenant, range, mappingVersion?, now?)` with one ordered tenant/date-bounded Waste scan from prior start through `as_of` and indexed correlated last-paid seeks, plus one ordered selected-window non-voided Spend-rate read through `db(env)`. Enforce same-day/date/send tie, NULL skip, future exclusion, void fallback, tenant isolation, and no write/cache/model/queue/cron work. (maps: WT2 every range/bound/tenant scenario; WT4 every valuation/fallback scenario; WT11 every read-shape/read-only scenario; WT12 shared-helper/no-parallel-model scenarios)
- [x] 1.5 Reduce each persisted Waste event once, ignoring loose quantity, into integer-cent last-paid value/estimate metadata, selected/prior/weekly monetary coverage, effective `leftovers` from non-NULL `prepared_from`, and separate department classification coverage. Acceptance: a pending non-leftover department never changes top-level/Tossed monetary status, but remains pending for department coverage and the independent qualifying-Spend/rate rules. (maps: WT4 rounding/zero/quantity/fallback scenarios; WT5 department-orthogonality/classification/arithmetic scenarios; WT6 every coverage/week scenario; WT8 Leftovers/pending scenarios)
- [x] 1.6 Compute Tossed value, exact event-count Items binned and N-bucket per-week value, matched-shape trend with exact reason precedence, qualifying Spend coverage, and Waste rate with Household excluded and Beverages included. Round source money once to cents and output ratios to one decimal; preserve NULL at documented zero/missing/incomplete denominators and apply no presentation threshold in the reader. (maps: WT5 zero-denominator/arithmetic scenarios; WT6 all KPI/trend scenarios; WT7 every classification, status, rate, and reason-precedence scenario)
- [x] 1.7 Build Department, Reason, and Avoidability breakdowns plus nonempty most-wasted item groups with exact count/money denominators, sparse/unvalued group visibility, classification/monetary coverage, canonical labels, representative event pairing, valued-before-unvalued ordering, every tie-breaker, cap six, and pre-cap total count. Derive each returned group status only as `unavailable`, `partial`, or `complete`; add no synthetic `empty` group. (maps: WT8 every breakdown/denominator/Leftovers/tie scenario; WT9 every sparse/representative/order/cap/status scenario)
- [x] 1.8 Author the exact deterministic empty/unavailable/partial/complete insight templates from the already-sorted aggregates, including fixed clause order, grammar, currency, and Avoidable share. Use the Department opening only when selected Department classification is complete; if any selected effective department is pending, use the leading Reason over all events so an incomplete subset is never claimed as household leader. Return the single `WasteAnalyzer` object with no LLM/random/hash/name heuristic, client reducer, or alternate reader. (maps: WT5 shared-shape scenarios; WT10 every insight/template/tie/pending-department scenario; WT11 deterministic repeated-read scenario)

## 2. Migration-backed production-reader coverage

- [x] 2.1 Add `packages/worker/test/waste-analyzer.test.ts` cases through exported `readWasteAnalyzer` over `sqliteEnv`'s full real migration chain for tenant isolation; Monday/Sunday; 4/8/12 selected and matched-prior edges; chronological bucket fields; 24-week bound; and future Waste/Spend exclusion. These production-reader assertions exercise the imported Spend bounds/date helpers, while the existing Spend tests must remain green after the export-only edit; add no duplicate helper oracle. (maps: WT2 every scenario; WT12 reader-production-entry/schema-compatibility/shared-helper scenarios)
- [x] 2.2 Add real-table cases for latest eligible date/send tie, same-day versus future, NULL-price skip, estimated and known-zero matches, per-source cents rounding, void fallback, amount/Spend-quantity/Waste-quantity ignored, cross-tenant denial, and absence of every prohibited fallback. Assert production outputs rather than recreate the selection/reducer in test code. (maps: WT4 every scenario; WT12 reader-entry/no-parallel-model scenarios)
- [x] 2.3 Pin all ten canonical reasons and exact five/five v1 classification, omission/current metadata, repeated explicit named-v1 replay, exact unknown-version validation, and ignored non-reason fields through the production registry/reader. Prove the published registry is frozen and exhaustive without inventing a v2 or unreachable future-current fixture. (maps: WT3 every currently production-reachable scenario; WT12 mapping/validation production-contract scenarios)
- [x] 2.4 Cover empty/unavailable/partial/complete selected, prior, and mixed weekly monetary states; estimated/unvalued counts; exact Items binned/per-week; trend/reason precedence; effective Leftovers; and zero/some/all pending departments. Explicitly prove pending department alone is orthogonal to Waste monetary status and a complete `Last-paid estimate`, while separately making Department classification/coverage incomplete and, when the selected Spend input is pending, making Waste rate incomplete. Pin `prior_incomplete` trend output without adding prior coverage fields. (maps: WT5 all coverage/orthogonality/arithmetic scenarios; WT6 every coverage/KPI/trend scenario; WT7 pending/input/reason scenarios; WT8 Leftovers/pending/denominator scenarios)
- [x] 2.5 Add fixtures/assertions for Household exclusion, Beverages inclusion, classified-Household-only exact-empty Spend, every rate edge/precedence; every Department/Reason/Avoidability denominator and sparse group; item amount/count/representative/order/cap ties and the nonempty `WasteItemStatus` union; and every exact insight string including partial grammar, some-pending and all-pending Department cases. Prove any pending selected effective department selects the all-events Reason opening rather than a leader from the classified Department subset. (maps: WT7 all scenarios; WT8 all scenarios; WT9 all scenarios; WT10 all scenarios)
- [x] 2.6 Through existing production capture/void operations and the same migrated reader, exercise late/backdated Waste and Spend, replayed Waste id, corrected/voided last-paid fallback, facts committed around successive reads, deterministic repeated reads, and no state change. Run the same code against a fresh full schema and fixtures representing previously migrated NULL/past rows; add no alternate schema, migration, duplicate registry, correction state, or snapshot promise. (maps: WT11 every late/duplicate/corrected/concurrent/compatibility/repeat scenario; WT12 fresh/current-schema reader scenarios)

## 3. Member API, MCP, and compatible profile adapters

- [x] 3.1 Extend the established `packages/worker/src/api/retrospective.ts` area with session-gated `GET /retrospective/waste`; accept only `range` and `mapping_version`, default to `8w`/current, emit both exact validation errors, call the shared reader, and return the direct typed object through `jsonWithEtag`. Keep existing `/api/*` dispatch and add no parallel API area, tenant input, direct D1 access, Wrangler entry, or writer. (maps: WT1 shared-object/read-only scenarios; WT2 auth/tenant scenarios; WT3 validation/replay scenarios; MA2 every endpoint/default/validation/auth/ETag/read-only scenario)
- [x] 3.2 Extend `packages/worker/test/api-member.test.ts` through the composed authenticated app for 8w/current defaults, all explicit ranges, exact `mapping_version` replay/unknown and invalid-range 400 bodies, direct shared shape, tenant non-overridability/isolation, unauthenticated rejection, ETag/304, and deterministic side-effect-free repeat GETs. (maps: WT3 named/default/unknown scenarios; WT5 all-transport-shape scenario; WT11 repeated-read scenario; MA2 every scenario; MA4 composed-API scenario)
- [x] 3.3 Extend `packages/worker/src/cooking-tools.ts` so `loadRetrospective` and the registered read-only `retrospective` tool add `.waste`, optional `waste_range` default `4w`, and optional `waste_mapping_version` default current while cooking `period`, Spend options/results, and profile inputs remain independent. Profile retrospective receives the same 4w/current object without a new selector; add no Waste writer. (maps: WT1 every MCP/profile/default/independence/no-writer scenario; WT2 auth/default scenarios; WT3 named/default validation scenarios; WT12 tool-contract scenarios; MA2 selector-name/shared-object scenarios)
- [x] 3.4 Extend `packages/worker/test/cooking-tools.test.ts` through the registered production tool/profile adapters for omitted/explicit Waste inputs, exact HTTP-versus-MCP selector names, all ranges, v1 replay/unknown validation, `.waste` equality with the shared object, unchanged cooking/Spend/profile fields, household isolation, and absence of a Waste writer. (maps: WT1 every scenario; WT2 tenant/range scenarios; WT3 default/replay/unknown scenarios; WT12 MCP production-entry scenario; MA2 API/MCP-name scenario)

## 4. Member Waste panel only

- [x] 4.1 In `packages/app/src/lib/data.ts`, consume the exported workerd-free `WasteAnalyzer`/range contract and add the exact typed Waste fetcher/query options for the established endpoint. Use key `['retrospective', 'waste', range]`, enable only on the active Waste tab, omit `mapping_version` for current policy, and keep the aggregate outside offline persistence; do not copy a response model or calculate an aggregate. (maps: WT5 all-transport-shape scenario; MA1 shared-range/query/offline scenarios; MA2 current/default/direct-object scenarios; MA3 active-query/no-client-model scenarios)
- [x] 4.2 In `packages/app/src/routes/_app.retrospective.tsx`, replace only the Waste placeholder while preserving the established Spend/Cooking behavior. Reuse the one canonical `?range` control, 8w replace-canonicalization, retained valid range, stable tab/panel ids, reciprocal ARIA, roving/wrapping Arrow/Home/End semantics, and named three-button `aria-pressed` group. (maps: MA1 every shell/range/tab/keyboard/distinct-model scenario; MA3 shared-control scenario)
- [x] 4.3 Render accessible `Loading waste analysis…`, structured `role="alert"` error with same-range Retry, distinct exact-empty state, unavailable state with exact count/classification facts, partial state with unmatched/estimated evidence, and complete state labelled `Last-paid estimate`. Show unmatched/estimated counts beside selected/Tossed value and each weekly, breakdown, or item-group value only where its returned coverage/counts supply the evidence. Pending Waste department classification alone must retain `Last-paid estimate` and appear separately as Department incompleteness; a separately pending selected Spend department may appear as returned rate incompleteness. Never show unavailable as `$0`, call value a receipt total, infer tossed quantity, invent prior coverage, or add a catch-all fallback. (maps: WT5 department-orthogonality scenario; WT6 empty/unavailable/partial/trend scenarios; MA3 every loading/error/empty/missingness/estimate scenario)
- [x] 4.4 Present returned Tossed value, Items binned/per week, Waste rate/reason, trend, chronological text-equivalent weeks, all three breakdowns with denominators/coverage, most-wasted rows, and exact insight without recalculation or reordering. Keep pending Department visibly orthogonal to the `Last-paid estimate` monetary label while separately exposing Department incompleteness and returned rate incompleteness. An unavailable trend must show its returned `current_incomplete | prior_incomplete | prior_zero` reason and current/prior known amounts without fabricated counts. Label Waste-derived money as last-paid estimates; if qualifying Spend is displayed, label it recorded/captured grocery spend rather than a per-toss estimate. Apply the red treatment only to an available returned rate at `>=10.0%`, with percentage text and no threshold styling for NULL. (maps: WT5-WT10 all presentation-relevant scenarios; MA3 KPI/text-equivalent/threshold/no-parallel-model scenarios)
- [x] 4.5 Add only Waste-scoped/shared Retrospective rules in `packages/ui/src/cookbook.css`: readable wrapping controls, ordered stacking cards, compact tall layout, labelled horizontal weekly overflow, and decorative geometry hidden from assistive technology. Use no `packages/app/src/styles.css`, canvas/chart dependency, hover-only content, offline fallback, or unrelated redesign. (maps: MA1 narrow/tall/no-library scenarios; MA3 responsive/text-equivalent scenarios)

## 5. Real seed, page object, and browser evidence

- [x] 5.1 Extend shared `packages/worker/admin/visual/seed.mjs` and `seed.d.mts` with stable tenant-owned Waste and eligible Spend facts in existing tables sufficient for a current 8w production result, range changes, Leftovers, reasons/avoidability, exact and sparse money, rate, trend, breakdowns, items, and insight assertions. Export only fixture identifiers/expected facts, not a parallel aggregate, and add no schema or browser-only model. (maps: WT2-WT10 production-data scenarios; WT12 production-entry/schema scenarios; MA4 primary-seeded-data/no-alternate-model scenarios)
- [x] 5.2 Extend `packages/worker/app/visual/pages/retrospective.page.ts` with semantic Waste tab/range/KPI/week/breakdown/item/insight/status/alert/retry locators, keyboard helpers, and desktop/narrow/tall capture helpers that address production markup and readable text. (maps: MA1 tab/range/accessibility/layout scenarios; MA3 loading/error/text/responsive scenarios; MA4 browser-production-entry scenario)
- [x] 5.3 Extend `retrospective.spec.ts` with a primary signed-in real-endpoint case proving 8w URL canonicalization, 4w/12w shared range changes, active-only queries, tab/range keyboard semantics, exact KPI/week/breakdown/item/insight text, qualified Waste-versus-recorded-Spend labels, available-only rate styling, Spend separation, and reviewed desktop/narrow/tall screenshots through the composed API and migrated reader. (maps: WT5-WT10 shared-output/order scenarios; MA1 all shell/responsive scenarios; MA3 active-query/text/threshold/control/responsive scenarios; MA4 primary-production-data scenario)
- [x] 5.4 Cover sustained loading, exact structured error and retry, distinct empty, unavailable, and partial presentations, including pending-department monetary-label orthogonality and an unavailable trend with exact `prior_incomplete` reason, with narrowly scoped endpoint interception only when the real seed cannot hold the presentation branch. Assert unmatched/estimated counts only from returned selected/weekly/breakdown/group evidence and assert no invented prior coverage. Every intercepted body must satisfy the exported production type and must not duplicate calculations or replace the primary seeded proof. (maps: WT5 department-orthogonality scenario; WT6 missingness/trend scenarios; WT7 unavailable-rate scenarios; MA3 loading/error/empty/unavailable/partial/no-parallel-model scenarios; MA4 typed-interception/no-alternate-model scenarios)

## 6. Living contracts, docs, and persona

- [x] 6.1 Sync the approved deltas exactly into `openspec/specs/spend-telemetry/spec.md` and `openspec/specs/member-app-core/spec.md`, preserve current-state language, and manually check every requirement/scenario against the implementation and reverse traceability above. (maps: WT1-WT12 all contract scenarios; MA1-MA4 all scenarios)
- [x] 6.2 Update `docs/TOOLS.md` with exact `waste_range`/`waste_mapping_version` MCP inputs, defaults, profile behavior, direct HTTP `range`/`mapping_version` distinction, returned shared fields/statuses/denominators/order, trend reasons, Waste-derived last-paid versus qualifying recorded-Spend semantics, validation, tenant authorization, and read-only/no-Waste-writer guarantee. (maps: WT1 all tool scenarios; WT3 selector/validation scenarios; WT5 shared-shape scenario; WT12 contract-alignment scenarios; MA2 selector/shared-object scenarios)
- [x] 6.3 Update `docs/SCHEMAS.md` with existing Waste/Spend columns and indexes plus last-paid/effective-Leftovers/avoidability read-time derivations, value/coverage missingness, and explicit absence of member value, Waste value/avoidability columns, fallback, migration, or history rewrite. (maps: WT3-WT8 derivation/coverage scenarios; WT11 compatibility/read-only scenarios; WT12 schema-alignment scenario)
- [x] 6.4 Update `docs/ARCHITECTURE.md` with the tenant-scoped 24-week-bounded Waste scan, indexed last-paid seeks, selected Spend-rate read, UTC/mapping replay/concurrency guarantees, and explicit no materialization/new index/new analyzer cron/scheduled work. (maps: WT2 bounded-read scenarios; WT11 every projection/concurrency scenario; WT12 architecture-alignment scenario)
- [x] 6.5 Update `packages/worker/AGENT_INSTRUCTIONS.md` so `cooking-retrospective` acts only on returned Waste facts and communicates last-paid estimate/partial/unavailable states without writing or inventing advice. Run the parse-only generated-plugin check and confirm no generated plugin artifact is edited. (maps: WT1 read-only/no-writer scenarios; WT10 server-authored insight scenarios; WT12 persona/plugin-alignment scenario)

## 7. Focused implementation verification and scope handoff

- [x] 7.1 Run focused Worker/app/UI/visual-harness typechecks and focused Vitest files for the migration-backed Waste reader, composed member API, registered retrospective MCP/profile adapter, exported wire contract, and directly changed member code, plus the existing focused Spend reader tests that guard the export-only helper edit; fix only failures attributable to this change. Do not substitute a pure reducer/helper-oracle test or run the later exact full non-browser gate here. (maps: WT12 every production-entry/test/shared-helper scenario; MA2 endpoint scenarios; MA4 API/browser-entry scenarios)
- [x] 7.2 Run the focused Retrospective Playwright spec and inspect desktop, narrow, and tall captures for semantics, loading/empty/error/unavailable/partial/complete truthfulness, readable text, available-only rate styling, wrapping controls, and weekly overflow. Do not substitute this focused evidence for the later full browser gate. (maps: MA1 all accessibility/layout scenarios; MA3 all presentation scenarios; MA4 primary/interception scenarios)
- [x] 7.3 Run `mise exec -- openspec status --change waste-analyzer`, `mise exec -- openspec validate waste-analyzer --strict`, `(cd packages/worker && mise exec -- node scripts/build-plugin.mjs --check)`, and `git diff --check`; verify the plugin check creates no diff and every checklist/spec reference remains aligned. Do not archive or mark later full gates, whole-PR review, CI, or PR/merge work complete here. (maps: WT12 contract/persona/plugin-alignment scenario; MA4 no-alternate-model scenario)
- [x] 7.4 Recount the complete merge-base diff and report exact files/additions/deletions plus unresolved findings to the main thread. Compare it with the exact anticipated 29-file set and stay within the frozen 24-32 files / 4,200-5,600 additions forecast; stop for approval above 40 files or 7,000 additions, and independently at more than 70 files or 7,000 additions. Confirm the `spend.ts` diff is export-only and no migration, index, cron, dependency, generated bundle, new cross-cutting mechanism, `styles.css`, parallel API/model, synthetic mapping version, or speculative state entered the diff. (maps: WT11 bounded/read-only/no-cross-cutting scenarios; WT12 production-design/compatibility requirements)
