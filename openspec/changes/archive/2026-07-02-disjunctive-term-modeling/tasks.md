## 1. Disjunction primitives (new module)

- [x] 1.1 Create `packages/worker/src/ingredient-disjunction.ts`: `isDisjunctiveTerm(s)` (standalone ` or ` token after folding `, or `/list commas; never matches `::`-detail-only text — test against the full id AND the base segment), `splitDisjuncts(s)` (D3 head-distribution: head = final fragment's tokens[1..], appended to fragments with fewer tokens than the final), `firstDisjunct(s)`, and `disjunctionResolution(term, vec)` returning the standard `Resolution` (node `{ base: term, detail: null, concrete: false, search_term: firstDisjunct(term), embedding: vec }`, edges `[]`, log `outcome: "novel"`, `detail { note: "disjunction_concept", disjuncts }`)
- [x] 1.2 Vitest `packages/worker/test/ingredient-disjunction.test.ts` — splitter table: `white or yellow onion` → [`white onion`,`yellow onion`]; `serrano or jalapeño peppers` → [`serrano peppers`,`jalapeño peppers`]; `anaheim or cubanelle peppers` → [`anaheim peppers`,`cubanelle peppers`]; `olive oil or butter` → no distribution; `serrano, jalapeño, or habanero peppers` → three distributed; non-matches: `half and half`, `pecans (halved and pieces)`, `80/20 ground beef`, `oregano` (no bare-substring `or` match), `jellies and jams (various)`

## 2. Capture-time disposal (part 1)

- [x] 2.1 `packages/worker/src/ingredient-normalize.ts` `resolveOne`: insert the disjunction gate AFTER the lexical fast path, BEFORE the floor check — `if (isDisjunctiveTerm(term)) return disjunctionResolution(term, vec)`; count via new summary field `disjunctionCaptured` (detect on `detail.note`, the `lexical_match` pattern)
- [x] 2.2 `packages/worker/src/ingredient-normalize.ts` `buildResolution` NOVEL branch: reject a validated canonical matching `isDisjunctiveTerm` → verbatim fallback with `canonical_rejected` + `canonical_reason: "disjunctive"`
- [x] 2.3 `packages/worker/src/ingredient-classify.ts` `SYSTEM_PROMPT`: add the rule line — an "X or Y" term/canonical is a purchase constraint, never a concrete product identity (no new few-shot)
- [x] 2.4 Vitest (`test/ingredient-normalize.test.ts`): a queued disjunctive term commits an abstract node + alias with NO confirm call and NO embedding-retrieval dependence beyond the batch vec; lexical fast path still wins for a punctuation variant of an existing disjunction concept (gate order); a disjunctive canonical proposal from the confirm falls back to verbatim with the rejection logged; summary counts

## 3. Shape sweep + membership reconcile (parts 2 & 3)

- [x] 3.1 `packages/worker/src/corpus-db.ts`: `applyDisjunctionRepair(env, plan)` with shapes `flip` (UPDATE `concrete=0, search_term=?` + `reshape` log `note: "disjunction_flip"`), `fold` (flip base if needed + representative merge child→base, log `merge` `note: "disjunction_child_fold"` — reuse `mergeIdentities`' cycle guard by delegating or replicating its resolve check), `reroot` (clear base's representative + point child at base + flip base, one atomic batch — the `repairSegmentOverflow` reroot shape rooted at the BASE), `mint-base` (INSERT OR IGNORE abstract base, embedding NULL, + point child)
- [x] 3.2 `packages/worker/src/ingredient-disjunction.ts`: `reconcileDisjunctions(deps, summary)` — (a) sweep: select surviving `source='auto'` `concrete=1` ids where `isDisjunctiveTerm(baseOf(id))`, group into families (base + children), apply 3.1 plans, cap writes at `DISJUNCTION_MAX_WRITES_PER_TICK` (10, shared with the membership inserts), skip+count `source='human'` (`disjunctionSkipped`); (b) membership: for every surviving `concrete=0` id where `isDisjunctiveTerm(id)`, split, resolve each disjunct through alias front door (lowercased exact) + representative chain; resolved-and-surviving ≠ concept and no edge either direction → `insertAuditedEdge(member, concept, "membership")` + log `edge_restore` `note: "disjunction_membership"` (`disjunctionEdges`); unresolved → collect and flush once via `enqueueNovelTerms` (`disjunctionEnqueued`, best-effort, never fails the tick)
- [x] 3.3 `packages/worker/src/ingredient-normalize.ts`: wire `reconcileDisjunctions` into `reconcileNormalization` after `reconcileSegmentOverflow`, before `reconcileCoResolution` (runs on empty queue too); extend `NormalizeSummary` + `emptySummary` with the six D12 fields; new deps (`insertEdge`, `enqueue`, `log`, `applyDisjunctionRepair` — reuse existing `identitySources`/`aliasTargets`/`allEdges` readers) threaded through `buildNormalizeDeps`
- [x] 3.4 Vitest (`test/ingredient-disjunction.test.ts`): the PRODUCTION SERRANO FIXTURE — seed base(concrete=1, rep→child) + child(`::form-diced`, survivor) + aliases (`serrano or jalapeño peppers`→base, `diced jalapeños`→child) + the structural edge; one pass ⇒ base survives abstract with `search_term "serrano peppers"`, child's representative → base, both aliases resolve to base, second pass plans nothing (idempotent/quiesced); flip-only fixture (`white or yellow onion`); mint-base fixture (orphan child, no base row); human node skipped; membership: disjunct with existing alias gains a born-stamped membership edge exactly once (rerun no-op), unresolved disjunct enqueued each tick until a node appears, no edge inserted when a reverse-direction edge stands; write cap honored
- [x] 3.5 Vitest: post-fold structural-edge interaction — with the folded family seeded, `auditEdges`' pre-pass sweeps the stale structural edge as a self-loop ONCE and the guarantee does NOT re-insert (child not surviving); extend `test/ingredient-edge-audit.test.ts` with this fixture
- [x] 3.6 THE RATIFIED OSCILLATION GUARD — `packages/worker/src/ingredient-edge-audit.ts` `ensureStructuralEdges` step (b): skip the guarantee insert when `view.resolve(base) === view.resolve(r.id)` (never guarantee a rep-resolved self-loop; closes the base-into-own-child inversion class generally)
- [x] 3.7 Vitest (`test/ingredient-edge-audit.test.ts`): reproduce the LIVE SERRANO OSCILLATION — inverted family (base rep→child, child surviving) + the stamped structural edge; one `auditEdges` pass sweeps the self-loop once (`self_loops_swept: 1`) and inserts NOTHING (`structural_restored: 0`); a second pass over the post-sweep state plans zero writes — churn quiesced BEFORE any shape sweep runs; and a healthy family (base resolves elsewhere) still gets its guarantee edge

## 4. Merge guards (D9)

- [x] 4.1 `packages/worker/src/corpus-db.ts` `readSkuCoResolutionPairs`: carry `concrete` for both survivors on `CoResolutionPair`; `packages/worker/src/ingredient-normalize.ts` `reconcileCoResolution`: skip (count `mergeSkipped`) when `pair.aConcrete !== pair.bConcrete`, before the confirm call
- [x] 4.2 `packages/worker/src/ingredient-reconfirm.ts`: reject a `same` merge whose survivor candidate is `concrete=0` — stamp + log the no-op (guard-rejection detail shape, like the distance guard); candidates need the concrete flag threaded from the identity read
- [x] 4.3 Vitest (`test/ingredient-normalize.test.ts`, `test/ingredient-reconfirm.test.ts`): mixed-concreteness co-resolution pair spends no confirm and merges nothing; reconfirm `same` onto a concept stamps a logged no-op

## 5. Alias re-audit parity (D10)

- [x] 5.1 `packages/worker/src/ingredient-alias-audit.ts`: before the classifier re-decision, an eligible variant matching `isDisjunctiveTerm` disposes deterministically — re-point to the disjunction concept (existing node via front door, else mint via `disjunctionResolution` with the variant's embedding), stamp, log with the audit marker + `note: "disjunction_concept"`
- [x] 5.2 Vitest (`test/ingredient-alias-audit.test.ts`): an un-audited disjunctive variant re-points with zero model calls; an already-correct mapping to the concept is a keep

## 6. Docs (lockstep, same pass)

- [x] 6.1 `docs/ARCHITECTURE.md` ingredient-normalization capture section: disjunctive terms are satisfaction concepts — the deterministic capture gate, the membership/shape reconcile sub-pass, the member-phrase `search_term` guarantee (state current behavior only, no history)
- [x] 6.2 `docs/SCHEMAS.md`: `ingredient_identity.concrete` semantics note — disjunctive-pattern ids are concept nodes; their `search_term` is a member phrase, never the disjunctive phrase
- [x] 6.3 Coordinate textual merge with `alias-target-convergence` if it has not yet landed (both touch these two docs; implementation serializes after it)

## 7. Verification

- [x] 7.1 Full battery: `aubr typecheck`, `aubr test` (vitest run), `openspec validate "disjunctive-term-modeling" --strict`
- [ ] 7.2 Post-deploy production fixture walk (`npx wrangler d1 execute grocery-mcp --remote --json` from `packages/worker`), after ≥2 cron ticks:
      (a) `SELECT id, concrete, representative, search_term FROM ingredient_identity WHERE id LIKE '% or %'` ⇒ `white or yellow onion` + `anaheim or cubanelle peppers` + `serrano or jalapeño peppers` all `concrete=0`, survivors, member-phrase search_terms; `…::form-diced` representative → base;
      (b) churn stopped: `SELECT COUNT(*) FROM ingredient_normalization_log WHERE term LIKE '%form-diced -[general]%' AND outcome IN ('edge_drop','edge_restore') AND created_at > <deploy_ts>` plateaus at ≤1 sweep;
      (c) disjuncts queued/captured: `SELECT term FROM novel_ingredient_terms WHERE term IN ('white onion','yellow onion','serrano peppers','jalapeño peppers','anaheim peppers','cubanelle peppers')` drains over ticks and membership edges appear in `ingredient_edge` (`to_id` = the three concepts);
      (d) grocery/pantry convergence: everett's `serrano or jalapeño peppers` rows' `normalized_name` re-keys to the base id via the grocery-pantry reconcile;
      (e) job health: `ingredient-normalize` summary shows the disjunction counts then returns to zeros (quiesced)
