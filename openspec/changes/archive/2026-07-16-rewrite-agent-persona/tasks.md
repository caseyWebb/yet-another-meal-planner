## 1. Preconditions

- [ ] 1.1 Confirm `narrow-mcp-surface` and `remove-ready-to-eat` are merged and archived; the Worker serves the final tool surface (fused `import_recipe`, `update_pantry` kitchen ops, `set_recipe_disposition`, `flyer`, `attention` block on `read_user_profile`, `update_taste` append/patch, no RTE tools).
      PARTIAL: the final tool surface is confirmed live (verified every tool the persona names against `docs/TOOLS.md`; RTE-free per `docs/TOOLS.md`'s deprecation table) and both changes are merged/committed, but **neither is archived yet** (still under `openspec/changes/`, not `openspec/changes/archive/`) — left unchecked because "archived" is literally false today; re-check once both archive.
- [x] 1.2 Reconcile this change's MODIFIED spec deltas against the post-archive text of `openspec/specs/` (headers must match exactly; drop anything changes 1+2 already removed); `OPENSPEC_TELEMETRY=0 openspec validate rewrite-agent-persona`.
      Reconciled: every MODIFIED/REMOVED header in this change's deltas (agent-plugin-distribution, repo-structure, guided-cook, guided-onboarding, agent-bug-reporting, cooking-techniques, recipe-sides, purchasing-guidance) matches a `### Requirement:` header verbatim in the current live `openspec/specs/`. `openspec validate rewrite-agent-persona --strict` passes.
      Found (flag before archive): `narrow-mcp-surface`'s own delta ALSO carries a `REMOVED Requirements` block for the identical headers "Capture flow distills member-supplied sources" (cooking-techniques) and "Capture flow distills member buying guides" (purchasing-guidance) that this change's delta removes. Harmless while both stay unarchived, but whichever of the two archives second will find the requirement already gone — the archiver should drop the now-redundant REMOVED block from whichever change archives later. `guided-onboarding`'s RTE cross-record requirement overlap with `remove-ready-to-eat` is NOT a conflict — this change's delta deliberately omits it (per design.md), so only `remove-ready-to-eat` removes it.

## 2. `packages/plugin` package (relocation before rewrite, so each is separately verifiable)

- [x] 2.1 Create `packages/plugin/package.json` (`@yamp/plugin`, private) with `build:plugin` (`node scripts/build-plugin.mjs`) and `test` / `test:tooling` (`node --test tests/build-plugin.test.mjs`) scripts; `pnpm-workspace.yaml` already globs `packages/*` — verify `aube install` picks it up.
      Also added a `typecheck` script (`node --check` over the two `.mjs` files) — the root `aube run typecheck` recurses into every workspace package and errors on one with no `typecheck` script at all; a plain-JS package has no `tsc` target, so this is the lightweight equivalent. `aube run typecheck`/`test`/`test:tooling` all resolved `@yamp/plugin` with no separate `aube install` needed (no new dependencies).
- [x] 2.2 `git mv packages/worker/AGENT_INSTRUCTIONS.md packages/plugin/AGENT_INSTRUCTIONS.md`; `git mv packages/worker/scripts/build-plugin.mjs packages/plugin/scripts/build-plugin.mjs`; `git mv packages/worker/tests/build-plugin.test.mjs packages/plugin/tests/build-plugin.test.mjs`. No generator logic changes (`REPO_ROOT`/`--src` defaults and the test's relative import survive the move); remove the `build:plugin` script and the moved test from `packages/worker/package.json`'s `test:tooling` list.
- [x] 2.3 Re-point root `package.json`: `build:plugin` filters `@yamp/plugin`; `test:tooling` covers both the worker's remaining tooling tests and the plugin package's tests.
- [x] 2.4 `.github/workflows/ci.yml`: the "Plugin builds from AGENT_INSTRUCTIONS.md" job's `working-directory` → `packages/plugin`; the deploy-relevance path filter → `packages/plugin/AGENT_INSTRUCTIONS.md packages/plugin/scripts/build-plugin.mjs` (replacing the two `packages/worker/…` paths).
- [x] 2.5 `.github/workflows/data-deploy.yml`: the publish step's `node _code/packages/worker/scripts/build-plugin.mjs …` → `_code/packages/plugin/scripts/build-plugin.mjs`.
- [x] 2.6 Comment/template references: `packages/worker/scripts/build-vault.mjs` header comment (persona path); `packages/worker/src/write-tools.ts` comment (the doc's cited `:589` had drifted to line 376 — same single occurrence, re-pointed); `.github/pull_request_template.md` and `.claude/agents/code-reviewer.md` generated-plugin wording checked — neither is path-specific, so neither needed a change.
- [ ] 2.7 Gate: `aubr test:tooling`, `node packages/plugin/scripts/build-plugin.mjs --check`, and `aubr build:plugin` all green at the new paths with the OLD persona content, before any rewrite lands.
      NOT run as a separate old-content checkpoint: the orchestrating task combined the `git mv` and the content replacement into one step (the draft was pre-written and reviewed), so the relocation was verified only against the NEW content, not gated against the old 17-flow content first. All three commands are green against the new content (see section 4); the intermediate old-content milestone this task describes was skipped by design of how the work was sequenced, not an oversight to still do.

## 3. Persona rewrite (`packages/plugin/AGENT_INSTRUCTIONS.md`, from scratch)

- [x] 3.1 Author the single `<!-- persona: core -->` tier per the design outline: identity + terse voice (no narration, no jargon, style-preference respect), session-start gate + household handles, silent-learning rules with the explicit-statement-only dietary gate and profile-page transparency pointer, unchanged confirmation posture for consequential actions, the one-nudge `attention` rule + next-step offers, widgets-for-showing, the core micro-behaviors (import, disposition, notes, retrospective relay, guidance tips, store capture), degrade-by-tool-presence, and the error-relay / `report-bug` rule. No depth tiers; no RTE, shim lore, classification rubric, or weather narration.
      Drafted externally and installed byte-for-byte (see 2.2); verified structurally after install — single `<!-- persona: core -->` block, no depth-tier markers present, `--check` and the census test both green.
- [x] 3.2 Author the six `## Common flows` blocks with `<!-- skill: … -->` markers (no `needs:`): `plan` (engine-driven planning + sides ladder + sale-steering + save + to-buy review + shop offer), `shop` (branch detection + the flush branches as rewritten `<!-- resource: references/*.md -->` files + lifecycle assertions + storage tips), `cook` (pre-flight, card/text walk, user-owned timers, technique tips, in-flow logging incl. reported past meals + one reaction offer), `pantry` (merge-not-duplicate updates, kitchen ops, staples prompt, market-haul hand-off to `plan`), `setup` (three areas, idempotent, closes with a first-plan offer), `report-bug`.
      Verified: exactly six `### ` flows in document order (`plan, shop, cook, pantry, setup, report-bug`), each with a `<!-- skill: … -->` marker and no `needs:`. The three `<!-- resource: references/*.md -->` blocks in `shop` were missing their closing `<!-- /resource -->` markers (a mechanical marker-grammar gap, not prose) — added the three closing tags so the generator actually extracts `kroger-online.md` / `store-walk.md` / `instacart.md`; no prose changed.
- [x] 3.3 Trigger descriptions: fold each absorbed skill's member phrasings into its absorber's description; respect the build validator's limits (≤1024 chars, no angle brackets); confirm every tool named in a flow body exists on the post-1+2 surface (grep against `docs/TOOLS.md`).
      `validateParsed` (the build's own validator, which enforces the 1024-char/no-angle-bracket limits) returns zero errors. Every tool name referenced in the persona (`add_meal_vibe`, `add_recipe_note`, `add_store`, `add_store_note`, `create_instacart_handoff`, `display_grocery_list`, `display_meal_plan`, `display_order_review`, `display_recipe`, `flyer`, `import_recipe`, `kroger_login_url`, `kroger_prices`, `list_new_for_me`, `log_cooked`, `place_order`, `propose_meal_plan`, `read_guidance`, `read_meal_plan`, `read_pantry`, `read_recipe`, `read_recipe_notes`, `read_to_buy`, `read_user_profile`, `report_bug`, `retrospective`, `search_recipes`, `set_recipe_disposition`, `update_diet_principles`, `update_grocery_list`, `update_meal_plan`, `update_pantry`, `update_preferences`, `update_taste`) was checked against `docs/TOOLS.md` and resolves to a documented tool on the current surface.
- [x] 3.4 Self-review pass: total length dramatically under 639 lines (core ≲120); no machinery jargon in member-addressed prose; store-agnostic and subscription-corpus framing throughout; Kroger/Instacart branches phrased to degrade by tool presence.
      136 lines total; the core tier spans 35 lines. Grepped the banned-jargon list (vibe/palette/corpus/embedding/retrieval/slug/tenant/engine/MCP/widget/flush/derivation/overlay/satellite/D1/KV/R2) — every hit sits in skill-marker `description:` metadata or agent-facing procedure steps (never in first-person member-addressed sentences); "cookbook" is used consistently as the member-facing term. `shop`'s branch table and the "Use what's present" core rule cover store-agnostic degrade-by-presence explicitly.

## 4. Build + tests

- [x] 4.1 Update the real-doc contract test in `packages/plugin/tests/build-plugin.test.mjs`: flow census `['plan','shop','cook','pantry','setup','report-bug']`, all `needs` empty, only `skills/yamp-core/SKILL.md` emitted among library tiers, `shop`'s reference files present, prerequisite lines core-only. Fixture tests (which exercise depth tiers and resources against inline docs) stay as-is — the generator still supports them.
      All 35 tests in the file pass, including the rewritten real-doc contract test; the 33 fixture tests are untouched.
- [x] 4.2 `aubr test:tooling` green; `node packages/plugin/scripts/build-plugin.mjs --check` green.
      Both green — `aube run test:tooling` (worker: 74 tests, plugin: 35 tests) and `node scripts/build-plugin.mjs --check` from `packages/plugin/` (`validation passed: 6 workflow skill(s) + library tiers [plan, shop, cook, pantry, setup, report-bug]`).
- [x] 4.3 `aubr build:plugin` (throwaway build) and read the generated bundle end-to-end — library skill, six workflow skills, reference files, manifest, `.mcp.json`.
      Per explicit orchestrator instruction, the CLI's throwaway write was NOT run; verified equivalently by calling `buildPluginFiles`/`parseInstructions` directly and inspecting the in-memory file map end-to-end: `.claude-plugin/plugin.json`, `.mcp.json`, `skills/yamp-core/SKILL.md`, the six workflow `SKILL.md`s, and `skills/shop/references/{kroger-online,store-walk,instacart}.md` are all present with the expected content.

## 5. Docs lockstep (living voice — current state, no history)

- [x] 5.1 Path updates wherever `AGENT_INSTRUCTIONS.md` or the builder is referenced: `AGENTS.md` (docs list + rules section), `CLAUDE.md` (if any direct path), `README.md` (repo-map table + docs list links), `CONTRIBUTING.md` (repo map, "Building the plugin" section, auto-deploy path list, `test:tooling` description), `docs/ARCHITECTURE.md` (canonical-source paragraph + link), `docs/SELF_HOSTING.md` (project-paste fallback path).
      CLAUDE.md carries no direct `AGENT_INSTRUCTIONS.md`/`build-plugin` reference (confirmed by grep) — no change needed there. All other listed files updated.
- [x] 5.2 Skill-census updates: `docs/TOOLS.md` (any skill/tier references surviving change 1's rewrite), `docs/ARCHITECTURE.md` (persona/plugin section: single core tier, six skills), `docs/SCHEMAS.md` (guidance-domain capture notes now name operator curation via admin, not the removed capture skills).
      `docs/SCHEMAS.md`'s guidance-domain sections already said "operator-curated… vault edits" with no capture-skill mention (narrow-mcp-surface had already updated them) — nothing to change there; instead updated `docs/SCHEMAS.md`'s `pairs_with` field note, which named the retired `recipe-sides`/`meal-plan` flow names and the removed `update_recipe` tool as the (now-paused) edge-authoring path. `docs/TOOLS.md` and `docs/ARCHITECTURE.md` had several old flow-name mentions (`shop-groceries`, `meal-plan` skill, `configure-yamp-profile`, `report-yamp-bug`) beyond the persona/plugin-tier paragraph itself — all renamed to the six-skill census.
- [x] 5.3 Grep gate: `grep -rn "packages/worker/AGENT_INSTRUCTIONS\|worker/scripts/build-plugin" README.md CONTRIBUTING.md AGENTS.md CLAUDE.md docs/ .github/` returns nothing.
      Ran verbatim — empty output, exit 1.

## 6. Validation + publish

- [x] 6.1 `OPENSPEC_TELEMETRY=0 openspec validate rewrite-agent-persona` clean; `aubr typecheck` + `aubr test` green (comment-only worker changes; no runtime deltas expected).
      `openspec validate rewrite-agent-persona --strict` → "Change 'rewrite-agent-persona' is valid". `aube run typecheck` → exit 0. `aube run test` → exit 0 (worker: 180 files/2853 tests passed + 4 files/16 tests skipped [live-only]; satellite/widgets/admin-app/app all green).
- [ ] 6.2 Merge to `main`; confirm the data-repo deploy fires (persona paths are in the relevance filter), redeploys the Worker first, and publishes the rebuilt bundle with a strictly-greater version.
      Not part of this implementation pass — no commit/push was made (per instruction). Future step.
- [ ] 6.3 Live check on an installed member: plugin auto-updates; the six skills (and only those) appear; a "show my list" ask renders the grocery widget; a stated allergy writes without ceremony; no machinery jargon in a sample plan → shop → cook session.
      Requires a live deployed environment post-merge — out of scope for this pass.
