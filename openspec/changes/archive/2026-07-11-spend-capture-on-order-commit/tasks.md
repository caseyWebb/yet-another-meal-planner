# Tasks — spend-capture-on-order-commit

Ordered by dependency. **Implements AFTER `pantry-disposition-foundations`** — this change consumes its `src/department.ts` module + `ingredient_identity.category` memo and extends its `ingredient-category` job; do not start section 2 before that change's implementation has landed those. **No spike tasks** — the production shape questions (what the send path can price, the lifecycle-history fixture, the migration numbering) were settled by the planning spike against production D1 (see design.md Context; DB name for read-only spikes is `grocery-mcp`, not the config's binding name). **Serial surfaces — do not implement in parallel with:** `pantry-disposition-foundations` (it lands first — `src/department.ts`, the `ingredient-category` job), `meal-dimension-foundations` (`update_preferences`/`read_user_profile` TOOLS+SCHEMAS sections), `brand-tier-model` (same preferences sections), and band 3's `order-review-rework`/`offline-stores-and-store-walk` (order-placement spec + the shared commit ops — they land after). Migration number = next free at implementation (`0050` as of planning, after the sibling's `0049`).

## 1. Migration + schema plumbing

- [x] 1.1 `migrations/d1/NNNN_spend_capture.sql` (next free number; `0050` as of planning): `order_sends`, `order_send_lines`, `spend_events` (DDL verbatim from design.md D1/D3 — `department` nullable on both line tables; including `idx_order_sends_tenant`, `idx_spend_events_tenant`, `idx_spend_events_item`); `ALTER TABLE grocery_list ADD COLUMN sent_in TEXT`; `ALTER TABLE profile ADD COLUMN weekly_budget REAL`. Purely additive, no backfill (production has zero lifecycle rows — spike-verified); assumes the sibling's migration (`ingredient_identity.category`) is already applied. Apply locally with `npx wrangler d1 migrations apply DB --local`.
- [x] 1.2 Thread `sent_in` through `GroceryItem` (`src/grocery.ts`), the read SELECT + row mapping and `groceryUpsertStmt` (`src/session-db.ts`). It is **not** accepted as input by `add_to_grocery_list`/`update_grocery_list`/the member grocery routes (no schema field, no patch key) — internal linkage only.

## 2. Department derivation — extend the sibling's `src/department.ts` + `ingredient-category` job

- [x] 2.1 `src/department.ts` (sibling-owned, extended in place): add pure `departmentForGroceryLine({ key, kind, domain }, memoLookup)` — `kind` `household`/`other` or a non-grocery `domain` → `household` (immediate, never pending); else memo hit → its value (any memo value, `household` included); else `null` = pending — and `COST_PER_MEAL_EXCLUDED = ["household", "beverages"]` beside the vocab. The memo lookup is the sibling's `ingredient_identity.category` read (alias → identity → representative), batched for the snapshot paths. Unit tests: override precedence, the "2x4 lumber" → `household` fixture (SCHEMAS.md's row), cold-id → `null`, `leftovers` never produced for a grocery line.
- [x] 2.2 `src/ingredient-category.ts` (sibling-owned, extended in place): add the spend-fill phase beside the existing waste-event stamp — fill `order_send_lines.department` and `spend_events.department` where NULL from the memo via `line_key` (NULL→value only, bounded, idempotent; no new job, no new `scheduled()` wiring, no new `AiActivity`). Unit tests: pending rows drain, a stamped value is never rewritten, replayed phase rewrites nothing; `job_runs` reports the fill counts like the sibling phases.

## 3. Send-record snapshot on the `place_order` flush

- [x] 3.1 `src/spend.ts` (part 1 — snapshot): `SendSnapshot`/`SnapshotLine` types and `snapshotStatements(env, tenant, send, lines)` returning prepared statements (`order_sends` insert + `order_send_lines` inserts, insert-or-ignore) for composition into the advance batch; `deleteSendStatements(sendId)` for the rollback compensation.
- [x] 3.2 `src/order-shapes.ts`: `PlaceOrderResult` gains `send: { recorded: boolean; id?: string; error?: string }` (leaf module, no workerd types).
- [x] 3.3 `src/order.ts` `placeOrder`: widen the `advanceInCart` dep receipt to `{ inserted, sendId? }` and pass it to `rollbackInCart`; report `result.send` honestly (recorded with id on success; `{ recorded: false, error }` when the snapshot build failed but the flush proceeded — design D4). Preview/empty-resolve short-circuit unchanged (no send).
- [x] 3.4 `src/order-tools.ts` `runPlaceOrder`: build the snapshot lines before the advance — per resolved line: prices from `ResolvedLine` (`unit_price` = promo when `isOnSale` else regular; `savings` via `deriveSavings`; `estimated: 0`), `department` via `departmentForGroceryLine` (memo batch-read for the line keys; kind/domain from the stored row by key, plan-derived lines are food; NULL = pending), `provenance` per design D6 (stored-list keys ∪ derived-plan keys ∪ non-empty `for_recipes` → `planned`; else `impulse`), `for_recipes` from the to-buy line. Thread stored-row kind/domain and the derived-need key set from the data already in hand (no extra reads). `makeAdvanceInCart`/`makeRollbackInCart` compose the snapshot/delete statements and the `sent_in` stamps into the existing batches (`advanceInCartRows`/`rollbackInCartRows` in `src/session-db.ts` gain an optional `sendId`). A snapshot-build throw degrades to a bare advance + `send.error` — it must never fail the flush.
- [x] 3.5 `place_order` tool description (`src/order-tools.ts`): state the send-record side effect, the `send` result field, and that the snapshot prices are send-time quotes (the existing SKU-not-price guarantee text extends naturally).
- [x] 3.6 Tests (`test/order.test.ts` / order-tools suites): snapshot rides the advance batch (send exists iff advance succeeded); rollback deletes it; preview writes nothing; provenance mapping across the four line origins (design D6 scenario); department stamping incl. a household row (immediate) and a cold id (NULL-pending); `send` reported honestly on a forced snapshot failure.

## 4. Send-record snapshot on the satellite receipt flush

- [x] 4.1 `src/ingest.ts` (order arm): on the first landing, build the send snapshot from the carted/substituted observations — send id = the order-list id, `store`/`location_id` from the order-list, `fulfillment: "satellite"`, `order_list_id` set; per line: `product.productId` → `sku`, `product.size` → `size`, `product.price` → `unit_price` (regular/promo/on_sale/savings NULL, `estimated: 0`), `provenance: "planned"`, `department` via `departmentForGroceryLine` (kind/domain from the stored row; a plan-derived issued id is food; NULL = pending); name = the advanced line's display. Compose into the existing `advanceInCartRows` call (thread `sendId`); insert-or-ignore makes the residual double-intake converge. Best-effort: a snapshot failure logs and never rejects the receipt.
- [x] 4.2 Tests (`test/satellite*.test.ts` / ingest suites): first landing persists the send + lines and stamps `sent_in`; replayed receipt converges (no duplicate lines); unavailable lines get no snapshot line; missing `product.price` → NULL-unknown fields; snapshot failure still lands the receipt.

## 5. The one spend writer + lifecycle hooks

- [x] 5.1 `src/spend.ts` (part 2 — materialize/void): `recordPurchaseAssertion(env, tenant, rows, occurredOn)` — for rows with `sent_in`, load their `(send_id, line_key)` snapshot lines and `INSERT OR IGNORE` `spend_events` copying verbatim (`amount = unit_price × quantity`, NULL when unpriced); rows without a linkage or line are skipped. `voidSpendEvents(env, tenant, rows)` — stamp `voided_at` where NULL. Both throw-free toward tools via the `db()` layer's structured errors.
- [x] 5.2 `src/session-db.ts` `updateGroceryRow` hooks, inside the shared op (never the tool/route): legal `in_cart → ordered` → `recordPurchaseAssertion` (after the row write; same request); `in_cart → active` → clear `sent_in`, no spend; leaving `ordered` (either direction) → `voidSpendEvents` + clear `sent_in` (the branch that already clears `ordered_at`). `advanceOrderedRows` (satellite mark-placed) → `recordPurchaseAssertion` for the advanced rows. `removeGroceryRow` untouched in behavior — removes never write spend (the op-homed negative guarantee; no shared receive op exists, and the spec binds any future receive-completing op to perform the assertion internally — design D3).
- [x] 5.3 Tool descriptions (`src/grocery-tools.ts`): `update_grocery_list` — the advance-records-spend guarantee + void-on-relist; `remove_from_grocery_list` — removes never write spend (ownership boundary: guarantees live in descriptions, choreography in the persona).
- [x] 5.4 Tests (session-db/grocery suites): materialize on the guarded advance (verbatim copy, exactly-once on replay attempts); manual `active → in_cart` then `ordered` writes nothing (no linkage); `in_cart → active` clears linkage and writes nothing; re-list of `ordered` voids and clears; satellite `mark_placed` materializes idempotently; the member PATCH route inherits all of it through the shared op (route-level test reusing the existing grocery API harness).

## 6. Weekly-budget preference

- [x] 6.1 `src/preferences.ts`: `weekly_budget` joins `DEFINED_PREFERENCE_KEYS`; validation — finite number ≥ 0, `malformed_data` otherwise. `src/profile-db.ts`: read/write the `profile.weekly_budget` column; assemble into the preferences object when non-null (the defined-scalar idiom). `src/write-tools.ts` apply path as needed. **Serial surface:** the same files/sections as `brand-tier-model` + `meal-dimension-foundations`.
- [x] 6.2 Tests: merge/validate/delete round-trip; rejection of negative/non-numeric; `read_user_profile` returns it.

## 7. Retrospective spend section

- [x] 7.1 Household-scoped `spend` section on the `retrospective` tool result (design D8): trailing 4 ISO weeks (`week_start`, `total`, `savings`, `events`, `estimated`) over non-voided `spend_events`; `awaiting_mark_placed` = current `in_cart` rows with `sent_in`; `weekly_budget` echoed. Plain SQL (a small `src/spend.ts` read or `retrospective-db` helper), independent of `period` like `underused`. Tool description updated.
- [x] 7.2 Tests: aggregation excludes voided events; week bucketing; awaiting count; empty-data resilience (all-null section fields on a fresh tenant).

## 8. Docs + persona lockstep (same pass)

- [x] 8.1 `docs/TOOLS.md`: `place_order` (send-record side effect, `send` return field, quote semantics), `update_grocery_list` (spend guarantees), `retrospective` (spend section), `update_preferences` (`weekly_budget` defined key). Current-state prose — no "now"/"no longer".
- [x] 8.2 `docs/SCHEMAS.md`: new spend-telemetry section (`order_sends`/`order_send_lines`/`spend_events` DDL + field semantics, the grocery-line derivation chain over the sibling-documented department vocab/memo — `household` overrides immediate, NULL = pending filled once by `ingredient-category`, the "2x4 lumber" → `household` fixture row, the provenance mapping, the last-paid read pattern over `idx_spend_events_item`); `grocery_list` gains `sent_in` in the session-state overview + item shape; preferences block + example gains `weekly_budget`.
- [x] 8.3 `docs/ARCHITECTURE.md`: the `ingredient-category` job's description gains its spend-fill phase; spend capture noted in the data model (snapshot-at-send / materialize-at-assertion, one writer; behavioral data tenant-scoped per D2).
- [x] 8.4 `AGENT_INSTRUCTIONS.md`: receive choreography (order skill "I picked up the groceries" + satellite §5 "Received, later") gains the advance-first step — rows still `in_cart` advance to `ordered` via `update_grocery_list` (the purchase assertion) before the removes/restocks (advisory choreography; the enforced guarantees live in the tool descriptions); a one-line note that "I placed the order" is what records the order's spend. `aubr build:plugin --check`.

## 9. Acceptance

- [x] 9.1 Gates: `aubr typecheck`; `aubr test`; `aubr test:tooling`; `aubr build:plugin --check`; `openspec validate "spend-capture-on-order-commit" --strict` (run the worktree's own `node_modules/.bin/openspec` from the repo root — the `~/.local/bin` shim can point at another worktree); `/code-review` before the PR. *(All gates green at implementation — migration landed as `0051_spend_capture.sql` (0050 was taken by the pantry sibling); `/code-review` runs with the PR.)* No app/admin UI touched → no Playwright deltas (`test:app`/`test:admin` only as regression if CI requires).
- [ ] 9.2 Production convergence checks, post-deploy (design.md Migration Plan): first real flush → send record + priced/stamped lines + `sent_in`; "I placed the order" → verbatim `spend_events`; `retrospective().spend` reflects them and `awaiting_mark_placed` transitions 0 → N → 0; a re-listed row voids; NULL-pending departments on send lines/events drain to 0 across `ingredient-category` ticks with no stamped value rewritten; `weekly_budget` round-trips. *(Deliberately open at implementation time — runs AFTER the deploy; every path was exercised locally against the real migration DDL in test/spend.test.ts + the order/satellite suites.)*
