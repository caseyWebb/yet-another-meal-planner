## MODIFIED Requirements

### Requirement: Code repository layout

The code repository (`caseyWebb/groceries-agent`) SHALL hold the Worker source under `src/`, the build tooling under `scripts/` (including the plugin builder `scripts/build-plugin.mjs`), the agent persona source `AGENT_INSTRUCTIONS.md`, the operator admin SPA under `admin/` (sources in `admin/src/`, the committed build in `admin/dist/`), the D1 schema migrations under `migrations/d1/`, the reference docs under `docs/`, the OpenSpec workspace under `openspec/`, and the test suites under `test/` (Worker) and `tests/` (build tooling). Reusable CI workflows that operator data repos call SHALL live under `.github/workflows/`. The code repository SHALL NOT commit a generated `plugin/` bundle or a `.claude-plugin/marketplace.json`; the plugin bundle is generated by the operator's deploy into the operator's data repo (see "One data repository as the deploy control plane and plugin marketplace host" and `agent-plugin-distribution`).

#### Scenario: Worker source and tooling are present

- **WHEN** the code repository is cloned and listed
- **THEN** `src/`, `scripts/` (with `build-plugin.mjs`), `migrations/d1/`, `docs/`, `openspec/`, `test/`, and `tests/` are present along with `AGENT_INSTRUCTIONS.md`, and no committed `plugin/` bundle or `.claude-plugin/marketplace.json` exists in the code repository

#### Scenario: The admin SPA generated tree is committed alongside its source

- **WHEN** the admin SPA is built
- **THEN** `admin/dist/` is generated from `admin/src/` and committed; the plugin bundle is generated into the operator's data repo by the deploy, not committed in the code repository

### Requirement: README and gitignore

The repository SHALL include a `README.md` explaining the project and how to use the repo, and a `.gitignore` covering Node artifacts, OS files, editor files, Cloudflare Worker secret files, and the Cloudflare **`.wrangler/`** local-state directory (which caches the account id and the account name). The operator data repository **and its template** SHALL likewise ignore `.wrangler/`, so local `wrangler` use never commits the account cache into a repository that may be made public.

#### Scenario: README explains the project

- **WHEN** a developer opens `README.md`
- **THEN** it explains what the grocery agent is and how to use the repository

#### Scenario: gitignore excludes secrets and noise

- **WHEN** Worker secret files, `node_modules/`, OS files (e.g. `.DS_Store`), editor files, and a `.wrangler/` directory are present in the working tree
- **THEN** `.gitignore` prevents them from being committed, in the code repository, the operator data repository, and the data-repo template

## ADDED Requirements

### Requirement: One data repository as the deploy control plane and plugin marketplace host

The deployment SHALL use **one** data repository on the operator's account (no GitHub org, no per-member repository), which MAY be **public**. It SHALL hold the operator's `wrangler.jsonc`, thin `.github/workflows/` callers of the code repo's reusable workflows, and the plugin marketplace the deploy publishes — `.claude-plugin/marketplace.json` plus the generated `plugin/` bundle with the operator's connector URL baked in — and nothing else operational: no corpus, no member data, and no GitHub App. Making the repository public SHALL be gated on it carrying no secret material in working tree or history (no committed `.wrangler/` account cache, no invite codes, no credentials); the sole Actions secret (`CLOUDFLARE_API_TOKEN`) is encrypted and SHALL remain unexposed by a visibility flip. Members SHALL NOT be required to own or operate any infrastructure (no Worker, no Kroger app) and SHALL NOT be required to have a GitHub account — identity is an operator-issued invite code, member lifecycle is managed in the Worker's Cloudflare Access-gated `/admin` panel, and adding the public marketplace in claude.ai requires no GitHub authentication.

#### Scenario: Data repo holds config, workflow callers, and the published marketplace

- **WHEN** the data repository is inspected
- **THEN** it contains `wrangler.jsonc`, thin workflow callers, and the published `.claude-plugin/marketplace.json` + `plugin/` bundle, with no corpus, no per-member subtree, and no GitHub App

#### Scenario: A public data repo carries nothing secret

- **WHEN** the data repository is made public
- **THEN** its working tree and history contain no `.wrangler/` account cache, no invite codes, and no credentials, and the encrypted `CLOUDFLARE_API_TOKEN` Actions secret is not exposed by the visibility flip

#### Scenario: A member owns no infrastructure and needs no GitHub account

- **WHEN** a member is onboarded
- **THEN** they need only a Claude.ai account, a Kroger account, and an operator-issued invite code — they add the operator's public marketplace without GitHub authentication, with no Worker deploy and no Kroger Developer app of their own

## REMOVED Requirements

### Requirement: One private data repository as the deploy control plane

**Reason**: The data repository no longer needs to be private — it carries nothing secret (corpus in R2, member data in D1, invites minted only in the Access-gated `/admin` panel, the sole Actions secret encrypted) — and it now additionally serves as the operator's **public plugin marketplace**. Replaced by "One data repository as the deploy control plane and plugin marketplace host."

**Migration**: Before flipping the repository to Public, remove any committed local state (`.wrangler/`), add `.wrangler/` to `.gitignore`, and scan history for pre-`/admin` invite codes or credentials. Members install the plugin from `/plugin marketplace add <operator>/groceries-agent-data`.
