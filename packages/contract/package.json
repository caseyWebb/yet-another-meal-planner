{
  "name": "@grocery-agent/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime-agnostic code shared by the Worker (workerd) and the satellite (Node): the recipe-parse spine and the satellite ingest wire contract.",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
