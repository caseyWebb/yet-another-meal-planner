{
  "name": "@grocery-agent/scraper",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "The home-network walled-source recipe scraper (walled-source-scraper): authenticates to paid recipe sites with the operator's own session, extracts recipes to the shared wire contract, and POSTs them to the grocery-mcp Worker's /admin/api/ingest.",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "grocery-scraper": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsx src/cli.ts run"
  },
  "dependencies": {
    "@grocery-agent/contract": "workspace:*",
    "fast-xml-parser": "^5.9.3",
    "playwright": "^1.61.1",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
