{
  "name": "@yamp/satellite",
  "version": "0.1.11",
  "private": true,
  "type": "module",
  "description": "The home-network satellite (satellite): runs off-cloud on the operator's own network, authenticates to walled recipe sites with the operator's own session, extracts recipes to the shared wire contract, and POSTs them to the yamp Worker's /admin/api/ingest under the recipe-scrape capability.",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "yamp-satellite": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsx src/cli.ts run",
    "build:helper-ui": "node scripts/build-helper-ui.mjs",
    "check:helper-ui": "node scripts/build-helper-ui.mjs --check"
  },
  "dependencies": {
    "@yamp/contract": "workspace:*",
    "fast-xml-parser": "^5.9.3",
    "playwright": "^1.61.1",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
