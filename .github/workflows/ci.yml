name: CI

# Upstream quality gate for the PUBLIC code repo: typecheck + both test suites on
# every push/PR. NO secrets and NO deploy — deployment is an operator action run
# from each operator's PRIVATE data repo (see .github/workflows/data-deploy.yml,
# the reusable workflow their thin deploy.yml caller invokes). Keeping this repo
# secret-free is the point of the consolidation: the data repo is the single
# control plane.

on:
  push:
    branches: [main]
  pull_request: {}
  workflow_dispatch: {}

jobs:
  no-open-changes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: No unarchived OpenSpec changes
        run: |
          open=$(find openspec/changes -mindepth 1 -maxdepth 1 -type d ! -name archive | sort)
          if [ -n "$open" ]; then
            echo "Unarchived OpenSpec changes must be archived before merging:"
            echo "$open" | sed 's|openspec/changes/|  - |'
            exit 1
          fi

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test (Worker)
        run: npm test

      - name: Test (build tooling)
        run: npm run test:tooling

      - name: Plugin skills are current with AGENT_INSTRUCTIONS.md
        # The committed marketplace plugin's skills/ are generated from
        # AGENT_INSTRUCTIONS.md — guard against drift. The manifest carries an
        # operator-specific connector URL and a per-commit version (0.1.<count>),
        # and .mcp.json is operator-specific too, so the comparison is scoped to
        # skills/ (the only part deterministically derived from the source doc).
        run: |
          node scripts/build-plugin.mjs --out /tmp/plugin-check --mcp-url __ci__
          diff -r plugin/grocery-agent/skills /tmp/plugin-check/skills

  trigger-deploy:
    # Auto-trigger the data-repo deploy on Worker-relevant changes to main.
    # Only fires on push (not PRs or workflow_dispatch) — the push trigger above
    # is already filtered to branches: [main], so no extra branch guard needed.
    # Gated on both CI jobs so a deploy never fires when tests are red.
    if: github.event_name == 'push'
    needs: [no-open-changes, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Check for Worker-relevant changes
        id: changed
        run: |
          git diff --name-only HEAD^ HEAD -- src/ wrangler.jsonc package.json package-lock.json \
            | grep -q . \
            && echo "relevant=true" >> "$GITHUB_OUTPUT" \
            || echo "relevant=false" >> "$GITHUB_OUTPUT"

      - name: Trigger Deploy Worker
        if: steps.changed.outputs.relevant == 'true'
        env:
          GH_TOKEN: ${{ secrets.DATA_REPO_ACTIONS_TOKEN }}
        run: gh workflow run deploy.yml --repo caseyWebb/groceries-agent-data
