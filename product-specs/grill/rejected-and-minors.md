# Rejected findings and minors

Adversarial grill, 2026-07-10. Section 1 lists the 25 candidate findings the verification pass REJECTED — these are settled non-issues; check here before re-raising anything shaped like them against the spec set. Verdicts: `already_covered` (the spec set/decision log already answers it), `immaterial` (true but not material pre-proposal), `wrong` (factually incorrect about the repo). Section 2 lists the 32 minor findings with resolution gists.

---

## 1. Rejected findings (25) — do not re-raise

### Curated public set mechanics must respect 'no data in this repo' — decide the delivery channel now
**Verdict: already_covered**

The finding restates story 01 §5 q5 verbatim — "Curated set mechanics: who maintains it (product repo? a yamp-operated feed?), how it updates, whether households can hide it" — and its proposed resolution simply picks one of the two options q5 already enumerates. The spec set's README explicitly scopes open questions for resolution "before or during proposal drafting," and the curated set lands in band 5 (00-overview: "Social layer... curated public set"), so an open q5 at the pre-proposal stage is by design, not a gap.

Both escalation claims that would make it material NOW are factually wrong: (1) The quoted "§4 ('curated set = operator corpus')" conflation does not exist — story 01 §4 never mentions the curated set; it discusses the `shared-corpus` collision and the self-hosted profile. The spec set already cleanly separates the two notions the finding worries about: README steer 3/D3 say a new household inherits "no operator corpus" while the curated set is "product-maintained," and D9 states the curated set applies to the SaaS profile ONLY — the degenerate self-hosted case gets today's shared corpus via the implicit all-to-all friend lens, with no curated set involved. (2) The band-2 dependency is wrong: page 01's cold-start/onboarding block is tagged "story 01 — not in mock; SaaS profile only per D9," i.e. it ships with band 5's lens integration, not band 2's "page redesigns over existing data" cookbook restructure. Also, the visibility-scope schema value is not blocked on the delivery channel — story 01 §1 already fixes the tier ("public-curated (product-maintained set, visible to all)"); how the content arrives doesn't change the enum.

One genuinely useful nugget survives, as an annotation not a decision: q5's "product repo?" option collides with the CLAUDE.md "no data in this repo" invariant, so when band-5 proposal drafting resolves q5, the yamp-operated feed ingested through the existing discovery/ingest machinery (identity-keyed dedup per S2, operator-disable/repoint) is the natural answer. Worth a one-line constraint note on q5 ("product-repo delivery would violate the no-data-in-repo invariant"), nothing more at this stage.

Files: /home/user/yet-another-meal-planner/product-specs/stories/01-households-and-friends.md (§1, §4, §5 q5), /home/user/yet-another-meal-planner/product-specs/DECISIONS.md (D3, D9), /home/user/yet-another-meal-planner/product-specs/pages/01-cookbook.md (§2 cold-start), /home/user/yet-another-meal-planner/product-specs/00-overview.md (band 5).

### Aisle-map / store-note sharing scope is needed in band 3 but deferred to band 5's lens decision
**Verdict: immaterial**

The finding's facts check out, but its escalation claim — "band 3 cannot defer this" — does not survive scrutiny, and the underlying question is already explicitly tracked as an open question in the spec set.

Facts verified: openspec/specs/in-store-fulfillment/spec.md does make `stores` a shared registry and `store_notes` shared-by-default (author-attributed, optional `private`, layout/location/stock tag conventions); story 01 (product-specs/stories/01-households-and-friends.md) never mentions store notes — its lens tiers (§1), collision list (§4: multi-tenancy, shared-corpus, group-insights, self-service-signup), and open questions (§5) all omit them; the overview does sequence story 04 in band 3 and the social layer in band 5.

Why it is not material now:
1. Band 3 lands BEFORE band 5, i.e. in the pre-household world where in-store-fulfillment fully defines store-note sharing: deployment-wide, shared-by-default, author-attributed. "Community-mapped" copy is honest under that model — the whole deployment IS the community. There is nothing undefined for the band-3 proposal to block on; it ships over today's semantics.
2. D9 (DECISIONS.md) guarantees the self-hosted profile — the only profile that exists until band 5 — reproduces today's shared behavior indefinitely. So band-3-shipped behavior is not transitional scaffolding that band 5 invalidates; it is the permanent self-hosted behavior.
3. Because visibility is a lens over attributed data (D2), band 3 forecloses neither band-5 answer: layout notes are already keyed by the shared store slug and author-attributed, so household-scoping or pooling later is a read-filter change, not data surgery. The deferral is architecturally safe.
4. The spec set already records the question exactly where its protocol wants it: story 04 §4 q2 and page 09 §4 q4, phrased as a directly answerable question ("per-household only, or pooled — keyed how?"). Per product-specs/README.md, open questions are resolved "before or during proposal drafting" — this one can be answered by whichever proposal (band 3 opportunistically, or band 5 necessarily) first needs to commit.

Residual true kernel, worth a cheap doc fix but not a major finding: story 04 §1's pointer "needs the story-01 lens decision" is dangling (story 01 never contemplates store notes), and story 01 §4's existing-spec collision list omits in-store-fulfillment's `store_notes`, so a band-5 proposer could miss re-scoping them. Adding `in-store-fulfillment` to story 01 §4 and softening story 04 §1's pointer to "band 5's lens change re-scopes store notes; band 3 ships today's shared-by-default semantics" fully repairs the inconsistency without pre-deciding anything.

If/when the scope IS decided, the finding's flyer-doctrine split is the right default: layout-class notes (`layout`/`location`/`stock`) are public-derived observations about a physical store → pooled deployment-wide by store slug (with the story-01 privacy rule applied: no author identity shown outside the household/friend lens — pooled maps surface content, not who mapped them); freeform notes and per-household nicknames take the household lens at band 5.

### Outbound email is a genuinely new external dependency with no sender, no secrets model, and no degradation posture defined
**Verdict: immaterial**

Repo facts all verified: packages/worker/src/email.ts is inbound-only (Email Routing newsletter handler), no outbound sender exists anywhere in the Worker, and the opt-in secret-gated precedent is real (NTFY_URL in docs/ARCHITECTURE.md:388, ACCESS_* fail-closed in docs/SELF_HOSTING.md:125, CF_ANALYTICS_TOKEN setup-card degradation in docs/SCHEMAS.md:1074). Cloudflare's native send-email binding genuinely cannot target arbitrary member inboxes. However, the finding is not material NOW and is not unanswered in the sense that matters: pages/13-account-and-security.md §1 explicitly states 'Requires an email sender (none exists today) and a defined recovery flow', and its open q1 names exactly 'Email infra choice and recovery-flow security model (magic link -> passkey enroll)' — so the 'silently promotes email to a credential' charge misreads the spec, which marks the flow 'presumably' and lists the security model as undecided rather than inheriting it. pages/08-people.md q2 already frames notifications as 'purely on-page? email if set?', i.e. the opt-in/defined-unset posture. The README scopes open questions as resolvable 'before or during proposal drafting'; outbound mail is confined to page 13 plus one optional page-08 notification, no other page/story depends on it, and the resolution is fully determined by repo doctrine — so a proposal author can settle it without operator input (the finding's own needs_user:false concedes this). Recommended handoff to the page-13 proposal: one opt-in operator secret pair (EMAIL_API_URL/EMAIL_API_KEY, any HTTP-API sender, vendor is a deploy-time choice); unset => recovery-email card renders 'not available on this deployment', export promises no mail, page-08 requests stay on-page-only; recovery flow = single-use short-TTL KV tokens (WebAuthn-challenge pattern), issuance rate-limited via src/rate-limit.ts, magic-link sign-in lands in a passkey-enrollment-required state, address change resets verification. Also note D9: under the self-hosted profile the operator can rescue lockouts manually, so sender-less deployments degrade acceptably; SaaS-profile recovery urgency belongs in that same proposal.

### 'Department' is load-bearing across grouping, spend, and waste but has no defined deterministic source — make it a captured facet on the ingredient-identity graph
**Verdict: wrong**

The finding's core premise — 'department has no defined deterministic source / no model on the read path / no spec says where a plain line's department comes from' — is factually wrong about the repo. A deterministic identity-graph department derivation is SHIPPED and SPECCED: packages/worker/src/to-buy.ts (enrichView + DEPARTMENT_KIND_ORDER) derives placement.department + department_label per to-buy line from the ingredient identity graph's parent edges (membership > general > containment, representative-resolved, curated label), pure D1, zero LLM/Kroger product calls, served to the member app via /api grocery ?enrich=1. openspec/specs/member-app-differentiators/spec.md (lines 141–147, 177–196) specs it explicitly ('identity-graph department fallback'; 'placements carrying department only' with no store; 'Aisle unknown' bucket sub-grouped by department; 'groups by department where derivable and by kind otherwise') — i.e. the finding's refined behavior ('Not mapped' = aisle-only; honest fallback for unplaced ids) is already the shipped/specced contract. docs/TOOLS.md line 443 documents placement.department/department_label. The 'agent judgment' citation is the agent-side voice-walk skill (in-store-fulfillment), not the member read path. The proposed fix (a classifier-captured department COLUMN on ingredient_identity nodes) would mint a second parallel department source contradicting the existing edge-derived design — the graph parents ARE the captured facet, emitted/converged by the existing capture + audit crons with update_aliases as the human override; the finding inferred absence from the node schema while the facet lives on edges. Residue that is genuinely open — analyzer department vocab for spend/waste breakdowns (open-vocab graph parents vs fixed taxonomy), pantry category/location autofill source, recipe-card kitchen-location grouping, a default storage-location facet — is already explicitly enumerated as open questions in the very pages cited (pages/05 q3, pages/06 q3 which literally lists 'ingredient-identity funnel' as a candidate, pages/02 §3+q3) and is proposal-stage material, resolvable by pointing those proposals at the existing funnel. Useful drafting note for the bands: proposals should retrieve department through the existing identity-graph derivation (and decide vocab normalization there), not invent client heuristics or a new node field; story 04 §3's 'placement comes from the active store's knowledge' should be read with the differentiators spec's graph-department fallback in mind (aisle = store knowledge; department = graph).

### Member identity keying and existing-tenant migration are unstated — the single deepest schema decision
**Verdict: immaterial**

Factually accurate about the repo (SCHEMAS.md session:<token>→{tenant}, webauthn user handle = tenant id, tenants registry = canonical lowercase username, grant tenantId, kroger:refresh:<tenant>, notes author — all confirmed), but the finding's blocker claim fails: both 'plausible wrong designs' are foreclosed by decided text. Handle-as-key contradicts story 01 §3 ('mutable display key over a stable member id (and stable tenantId) — rename must not break links, requests, or grants'); re-keying tables/new household id contradicts story 01 §1 ('existing tenant becomes the household... no data movement'), §4 ('isolation boundary unchanged; member-scoped tables gain a member key within the tenant'), and D9 ('no tenant/corpus data surgery; self-hosters see no change beyond gaining household members'). Resolution points (1) and (4) merely restate these decided steers. The genuine residue — members table shape, founding-member id mapping, (tenant, member) in sessions/grants — is operator-taste-free engineering design that the finding's own resolution locates 'in the band-5 proposal's design doc', i.e. proposal-stage work, which the README's open-question doctrine ('before or during proposal drafting') and story 01 §5 q6 already anticipate. CLAUDE.md's workflow double-guards it: planning must resolve its own unknowns (no spike deferral) and the main thread ratifies the design doc before implementation. Nothing consumes the member PK before band 5 (bands 1–4 member-agnostic; 6–7 depend on 5), so nothing is blocked pre-proposal.

### No consolidated member-vs-household scope map; several domains are unlisted and genuinely ambiguous
**Verdict: already_covered**

The finding's headline ("no consolidated scope map; domains never scoped") is refuted by the spec set's default+exceptions rule, which scopes everything deterministically: S1/story 01 §1 sets the default (per-tenant → household-shared) with an enumerated member-level exception list, §4 gives the mechanism ("member-scoped tables gain a `member` key within the tenant"), and docs/SCHEMAS.md already labels tables per-member vs per-tenant. Walking the finding's "ambiguous" list: (1) Kroger link — explicitly decided household in story 01 §1 ("pantry, meal plan, grocery list, stores, Kroger link, staples"), so the claimed scoping trap is a page-level labeling refinement, and pages/09 q2 already carries Disconnect semantics as an open question; (2) taste_derived/discovery_matches — SCHEMAS documents both as per-member ("one row per member"; "the member the sweep matched it to") and taste_derived derives from member-level profile.taste, so the re-key is directly entailed by §1+§4 — the "household-average sweep" failure requires ignoring both documents; (3) satellite/ingest-key authority — pages/12 q1 asks exactly this; (4) export scope — pages/13 §1 + q3 ask exactly this; (5) spend — decided household in story 01 §2; (6) ready_to_eat catalog, last_planned_at, rotation, retrospective_prefs, pending_proposals — take the S1 default (household) with zero schema impact since the tenant key is reinterpreted, and the plan being household makes its watermark/prefs coherently household; (7) new-band tables (spend_events, walk_sessions) — bands 1–4 precede the social layer (band 5 per 00-overview sequencing), tenant-keyed tables are household-correct by default, and the member key only exists once band 5 lands, so band 5's proposal is the natural, standard place to produce the enumerated scope map as design output. The only genuinely underdetermined residue is small proposal-drafting material: whether ready_to_eat favorite/reject marks mirror the member overlay split (hooked by story 01 q6 "which tools become member-aware"), and the Account-tab Kroger card's household labeling — both are band-proposal details, not pre-proposal gaps, and the finding's own proposed table rows for them are the obvious inferences a proposer would draw from the existing exception list. The finding's evidence is factually accurate (SCHEMAS does say "pure personal taste"; Kroger appears on both tabs) but its conclusion that scoping is unanswered is wrong; the proposed normative table is a useful artifact for the band-5 (social layer) proposal's design doc, not a spec-set fix needed now.

### The profile/preferences doc needs a field-by-field member-vs-household split the mock's single page obscures
**Verdict: already_covered**

Factually accurate but substantively answered. (1) The field-by-field split is derivable mechanically from stories/01 §1 (decided, = S1): default rule "everything per-tenant today is household-shared" + explicit member-level enumeration including "taste profile & dietary principles" — applying it to the SCHEMAS.md profile row reproduces the finding's proposed household_profile/member_profile assignment exactly; the concrete table split is proposal-stage design.md work, and stories/01 §4 already specifies the mechanism ("member-scoped tables gain a member key within the tenant"). (2) Story 03's "member-set weekly budget" vs "one household-level $N/week" is a wording ambiguity the authoritative Budget bullet already resolves (household-level; "member-set" = set by a member — identical to the finding's own fix), and DECISIONS.md's precedence rule handles pages/09 §1's stale "Taste tab effectively unchanged" line the same way. (3) The dietary-union rule is genuinely unstated, but under tenant=household the dietary {avoid,limit} tokens remain tenant-scoped and gate exactly as today — the only union question is member-level diet_principles feeding the shared plan, which has one sane answer (finding itself says needs_user: false), falls inside stories/01 §5 q6's existing open-question family, and per the README is resolvable during proposal drafting; the households proposal cannot be written without answering it. Cheap non-blocking polish: add one line to stories/01 §1 ("the shared plan gates on the union of all members' dietary principles") and reword story 03 §1 to "household-level weekly budget, editable by any member."

### Vibe palette scope is undecided; the household plan needs ONE palette
**Verdict: already_covered**

Answered by stories/01 §1 (decided, D1): "Everything that is per-tenant today ... is thereby household-shared — no new sharing layer, no data movement", with member-level state splitting off only "where the mockup demands it" via a closed list (favorites/rejects, taste profile and dietary principles, cook-log authorship, notes authorship, feed follows, passkeys/sessions, nicknames) that excludes vibes. night_vibes/night_vibe_derived/vibe_satisfaction/pending_proposals are all per-tenant today (docs/SCHEMAS.md:215-259) and the mock renders one palette with no member dimension (conceded in the finding's own evidence), so the decided default yields exactly the finding's proposed resolution: ONE household palette, tenant keying unchanged, no member key needed for story 02's meal-column work. The SCHEMAS 'per-tenant PRIVATE ... never shared' wording remains correct read as never-shared-across-tenants (not through the friend lens); rewording to 'household-scoped' is routine docs-in-lockstep work already implied by stories/01 §4's multi-tenancy collision entry ('one member' becomes 'one household'), owned by whichever band lands households. No open decision remains; the finder's own needs_user=false corroborates.

### Cooking log scoping: household-keyed with member authorship, or the household plan machinery breaks
**Verdict: already_covered**

The scoping question is answered by the spec set's plain reading of steer S1's elaboration. Story 01 §1 sets the default "everything per-tenant today is household-shared — no data movement" and its member-split list names "cooking log AUTHORSHIP" (not "cooking log"), in deliberate contrast to "favorites/rejects OVERLAY"; the parallel "notes authorship" item maps to the existing pattern (household-visible records with a member author column, per docs/ARCHITECTURE.md attributed records). Story 01 §2 states it outright: "spend is household-level…; cook log rows are member-authored." Story 01 §5.6 frames the agent-side question as "log_cooked author" (an attribution param). Pages/07 §3 keeps retrospective aggregates "All household-scoped." No text anywhere suggests a member-scoped log. The feared fragmentation cannot arise under this reading: last_cooked/resurface key off tenant=household; vibes and meal_plan are NOT in the member-split list, so vibe_satisfaction/cadence debt and the log_cooked same-transaction plan clear stay household automatically; trending is tenant-level counts. The residual items in the proposed resolution (nullable member column, founding-member backfill, restating the dedupe key) are proposal-drafting mechanics — and story 02's dedupe key per-(date, meal, type, recipe) over an implicitly tenant-scoped table already yields household-wide dedupe by construction; per-member dedupe would be a deviation from the written key, not an ambiguity in it.

### meal_plan's PRIMARY KEY (tenant, recipe) constrains the grid, duplicate-at-commit, and projects — never mentioned
**Verdict: already_covered**

The finding's facts are right (docs/SCHEMAS.md meal-plan section: PRIMARY KEY (tenant, recipe); log_cooked clears by recipe in the same transaction) but its material claim — that no spec pins the one-row-per-recipe model and an implementer must decide duplicate semantics unilaterally — is already answered. (1) The living contract decides it: openspec/specs/member-app-propose/spec.md line 55 requires commit as "idempotent upserts keyed by recipe slug (replay-safe; an already-planned recipe merges rather than duplicating or erroring)", with the scenario at lines 62-65 mandating "upserts by slug without duplicating the row, and the member is told those nights were already planned" — exactly the finding's proposed duplicate-at-commit resolution, and product-specs/README.md says openspec/specs wins over page text, so pages/04 q4 already has an authoritative answer (upsert + feedback; the mock's silent skip is a mock bug vs. contract). (2) pages/03-meal-plan.md §2 states the recipe-as-row-identity behavior twice as requirements: "a recipe already in the plan moves (sides preserved)" and the unscheduled add "re-tags an existing row's meal if the recipe is already planned" — the one-row model is spec text, not an unconnected mock observation. (3) The genuinely open remainders are logged open questions the README designates for resolution before/during proposal drafting: projects representation is pages/03 q1 verbatim (read_meal_plan vs parallel list, grocery projection, lifecycle), and story 02 §2 already mandates the coordinated D1 + docs/TOOLS.md pass where the migration (meal column; PK fate) is necessarily confronted against docs/SCHEMAS.md. The finding's refined resolution (projects as meal = NULL plan rows riding the existing to-buy derivation; sidebar counts meal IS NOT NULL) is a good candidate answer for q1 that the proposal drafting can adopt, but it resolves a logged question rather than exposing an unlogged gap. No operator call needed: the surviving items are engineering-level and the duplicate/identity product call is already made in the living contract and page spec.

### recipe_notes' boolean `private` cannot express the story-01 tiers; migration must preserve D9's reduction
**Verdict: already_covered**

Factually accurate (SCHEMAS.md recipe_notes/store_notes carry `private INTEGER` with non-private notes read deployment-wide) but normatively redundant. (1) The tier-set + default question is verbatim stories/01 §5 q2, an explicitly tracked open question the README designates for resolution "before or during proposal drafting" — this finding is a pre-drafted answer to a tracked question, not an untracked gap. (2) The D9-preservation constraint the finding fears will be "silently" broken is already authoritative and already scoped to notes: DECISIONS.md D9 ("self-hosters see no change", "no tenant/corpus data surgery") + stories/01 §1 ("Notes and cook-log-derived signals flow through the same lenses") + §4 ("reproduces the deployment-wide behavior exactly ... migrate with zero data surgery"), and DECISIONS.md's header makes the log win over any conflicting proposal text — there is no silent-breakage path. (3) The proposed migration mapping (private=0 → 'friends' under implicit all-to-all friendship) is the unique D9-satisfying mapping, i.e. forced/derivable at proposal time, not a decision needing capture now; boolean→enum mechanics are proposal-drafting material. (4) pages/02 §2 does not lock in the binary composer — it says notes are "visibility scoped per story 01 tiers", deferring to the open question. The finding's genuinely open residue (new-note default; whether a public tier exists v1) is exactly q2 and will reach the operator at the scheduled decision point; its recommendation ('friends' default, no public tier v1, enum shared with store_notes) is a sensible candidate answer to carry into that proposal.

### Founding-member materialization for existing tenants is undefined — and the passkey user-handle contract forces member id = legacy tenant id
**Verdict: wrong**

The finding's severity-carrying mechanism is factually wrong about the repo. It claims passkey assertion resolution depends on the WebAuthn user handle, so fresh member ids would brick every enrolled passkey. In fact the user handle is write-only: it is stamped on the authenticator at enrollment (passkey-auth spec, enrollment requirement) but never consulted at login. The very spec the finding cites defines resolution by credential id — passkey-auth 'Requirement: Passkey authentication': 'verify the returned assertion against the stored public key for the asserted credential id, resolve the credential's tenant'. The code confirms: packages/worker/src/webauthn.ts:177 resolves via getCredentialById(response.id) and its header comment (lines 8-11) says 'the member is resolved from the asserted credential id'; src/api/passkey.ts:117 then resolveTenant(assertion.credential.tenant). grep for userHandle/user_handle across packages/worker/src, packages/app/src, packages/ui/src: zero hits; @simplewebauthn verifyAuthenticationResponse is called with no expected-user check. So 'member id MUST equal legacy tenant id' is not a forced constraint — the migration only needs the webauthn_credentials/session/grant rows mapped to the founding member, which is exactly the member-key backfill already flagged. The residue ((a) member-key backfill, (b) legacy tenantId-only grant resolution) is already covered: story 01 §1 enumerates which state splits to member scope, §4 says member-scoped tables 'gain a member key within the tenant' and explicitly assigns these collisions 'to resolve in proposals', §5 q6 flags the token-level split; D9 ('self-hosters see no change beyond gaining household members', 'no tenant/corpus data surgery') already binds any proposal against breaking auth or forcing re-connects at the steer level; and the multi-tenancy spec's idempotent tenants-registry backfill is the in-repo precedent the pipeline-convergence doctrine prescribes. Per the product-specs README, such mechanics are proposal-drafting work. A proposal MAY still adopt founding-member-id = tenant-id as a tidy convention (it makes grant/session resolution a trivial fallback), and the profile column-by-column household-vs-member split is a worthwhile proposal artifact — but nothing here is a blocker now, and no autonomous session following the cited specs would brick auth by picking UUID member ids.

### The near-term deployment target (self-hosted vs hosted SaaS) is never declared, and the profile flag's home + default are unspecified
**Verdict: already_covered**

The finding's headline — "the near-term target is never declared and specs silently assume one or the other" — is substantively answered by D9 (product-specs/DECISIONS.md) plus the specs' own logged open questions; what remains is derivable mechanics fine to settle during proposal drafting.

(1) Near-term target: D9 pins the behavioral contract the finding wants declared: profiles are long-term deployment configuration; the self-hosted profile must reproduce today's experience exactly ("self-hosters see no change beyond gaining household members", "no tenant/corpus data surgery for existing deployments"); friends UI, empty-corpus-on-join, and the curated set are explicitly SaaS-profile-only. The pages are NOT silently assuming a profile — they gate explicitly where the profiles diverge (product-specs/pages/08-people.md §1 "Profile gating (D9)"; product-specs/pages/01-cookbook.md cold-start "SaaS profile only per D9"). Nothing in the spec set commits to a near-term SaaS launch, so the absent billing/ops story is consistent scope, not a gap; and since the only deployments that exist are self-hosted, the repo's acceptance doctrine (verify against production after deploy) already fixes what bands validate against. (2) The "silent assumption" list dissolves on inspection: curated-set operatorship is already logged open (product-specs/stories/01-households-and-friends.md §5.5 "who maintains it (product repo? a yamp-operated feed?)"); email infra is already logged open (product-specs/pages/13-account-and-security.md §3.1) and recovery email is profile-agnostic anyway; handle directory + household-join requests + nicknames apply under self-hosted too (pages/08 hides only the FRIENDS section); Instacart credential acquisition is already assigned to the pre-proposal D7 spike (product-specs/stories/04-store-adapters-and-fulfillment.md §4.1 "auth model"); the Connect modal and satellites "operator superset" are profile-agnostic — a SaaS deployment also has an operator. (3) Flag default: "unset ⇒ self-hosted" is not an open decision but a one-line entailment of D9's zero-change consequence — any proposal choosing otherwise would violate a decided DECISIONS.md entry, which wins over all page/story text. (4) Flag home: the finding's "cannot be a code-level var" is overstated — packages/worker/scripts/merge-wrangler-config.mjs indeed never propagates code-repo vars, but the standard pattern (operator-owned var read with an in-code default, e.g. env.DEPLOYMENT_PROFILE ?? "self-hosted"; the deploy already injects vars via --var) makes a var perfectly workable, as the finding's own resolution concedes; var-vs-D1-operator_config is a mechanical design detail for the band-5 proposal, exactly the "resolve during proposal drafting" class the product-specs README defines. No operator taste is required: the default is entailed by D9 and the validation target is determined by deployment reality. Cheap hygiene the band-5 proposal can do without asking: record the one-line derivation (operator-owned config; unset ⇒ self-hosted; SaaS-only surfaces exercised via flag-flipped test/staging runs) in DECISIONS.md when the flag first ships.

### New deploy-config surface: choose the outbound-email mechanism now (send_email binding vs HTTP-provider secret) and inventory the other binding implications
**Verdict: immaterial**

Every technical claim verifies: only inbound email exists (src/email.ts + the email() export; docs/SELF_HOSTING.md); merge-wrangler-config.mjs is a strict per-key allowlist so a send_email binding would be silently dropped without an explicit line; r2_buckets is copied verbatim (out.r2_buckets = code.r2_buckets), so a second EXPORTS bucket entry needs no allowlist change; NTFY_URL/NTFY_TOKEN establish the optional-secret posture (wrangler.jsonc lines 3-8); the KV fixed-window limiter exists (src/rate-limit.ts). BUT the decision point is already recorded exactly where it belongs: pages/13-account-and-security.md line 13 states "Requires an email sender (none exists today)" and open question 1 is verbatim "Email infra choice and recovery-flow security model"; pages/08 q2 flags the notification path. product-specs/README.md (lines 30-32) designates open questions for resolution "before or during proposal drafting" — and the finding's own resolution says to decide "in the account-band planning", i.e. during proposal drafting. The silent-drop trap it warns about is documented doctrine in CLAUDE.md, in the merge script's own comments, and guarded by regression tests; CLAUDE.md's working mode already forces planning to resolve such unknowns and to serialize shared-scheduled() work. Nothing in the pre-proposal spec set is wrong or missing; no sequencing or story model depends on the mechanism choice; the finding itself concedes needs_user=false. Its content is high-quality planning input for the account band, not a spec-set gap — carry the refined resolution into that band's proposal.

### Curated-set distribution: concrete shape consistent with 'no data in this repo' — upstream public corpus pulled by a reconcile arm, provenance-tagged
**Verdict: wrong**

The finding's core thesis — that repo doctrine tightly constrains curated-set distribution to a public-upstream-pulled-by-a-new-reconcile-arm, and that alternatives (committed seed, manual rclone) violate doctrine — misreads the repo contracts. (1) rclone is a sanctioned corpus-authoring path, not doctrine-violating surgery: docs/SELF_HOSTING.md §2 documents rclone seeding/bulk edits, CLAUDE.md endorses it, and openspec/specs/r2-corpus-store/spec.md makes human-direct R2 edits a first-class validated flow ("Human-direct edits get eventual, surfaced feedback"); the "never manual surgery" invariant governs data-defect convergence, not content authoring. (2) The committed-seed risk is already guarded by CLAUDE.md's "There is no data in this repo," loaded into every session. (3) The proposed upstream-pull mechanism solves a cross-deployment distribution problem that DECISIONS.md D9 removed: the curated set is SaaS-profile-only, and the product team IS that deployment's operator — so "product-maintained curated set" reduces to ordinary recipes authored into the SaaS deployment's one monolithic corpus via existing surfaces (vault sync, agent import, rclone), tagged with the already-specced `public-curated` visibility tier and converged by the existing reconcile. No new reconcile arm, manifest, or upstream identity scheme is required. (4) The resolution's "operator opt-in config for self-hosted deployments" contradicts D9 ("the curated set appl[ies] to the SaaS profile only"). (5) The dedup/join-the-row semantics it proposes are verbatim S2/D2 (already covered), and the genuinely open residue of story 01 §5 q5 (who curates day-to-day, update cadence, hideability — noting per-member reject already hides via existing overlay semantics) is exactly what product-specs/README.md designates for resolution during proposal drafting by the social-layer proposal — immaterial pre-proposal. If the drafting session wants a one-line guardrail, it is: "curated set = ordinary corpus rows in the SaaS deployment authored through existing surfaces with the public-curated tier; never committed to the code repo" — which follows directly from CLAUDE.md + D2 + D9 and needs no new decision.

### Invite-link mechanics are unspecified: token semantics, public route/endpoint contract, and delegated allowlist authority
**Verdict: already_covered**

The finding mostly restates deferrals and open questions the spec set already carries, and its two "deeper" legs are factually off about the repo.

(1) Mechanics "unspecified" — deferred by design, and the house machinery already exists. stories/01 §3 already pins the record shape (`per-invite minted links carrying {inviter_member, tier, expiry/one-time}`) and explicitly routes the rest: "Coexists with the operator-minted invite codes (bootstrap) and group signup codes; reconcile in the change that builds this." pages/08 §4 q3 already asks the revocation question the finding raises ("Cancel semantics for a sent invite link (revoke the link?)"). Token entropy/format, default expiry, one-time-vs-multi, and rate limits are proposal-time engineering fully determined by the existing openspec/specs/self-service-signup/spec.md SHALLs (atomic claim-and-spend, per-IP fixed-window limiter, uniform no-oracle rejection, "the cap and expiry are the standing abuse bounds") — the finding's own resolution is "reuse the self-service-signup machinery" and its own needs_user is false, i.e. it concedes a planning session derives this mechanically. Per product-specs/README.md, open questions are meant to be resolved "before or during proposal drafting"; pre-proposal, this is immaterial.

(2) "New PUBLIC endpoint" / run_worker_first — factually wrong about the repo. A public, non-session `/api` redemption endpoint already ships: packages/worker/src/api/signup.ts is mounted un-gated in packages/worker/src/api/app.ts (line 44, "redeem a group invite code + chosen username → new tenant + session"), so member-api's session-gating enumeration already coexists with a public signup surface — invite-link redemption is a fork of that endpoint, which stories/01 §4 explicitly lists as a collision to resolve in proposals ("signup-via-household-invite joins an existing tenant; signup-via-friend-invite... creates a new tenant plus the edge"). And `/api` + `/api/*` are already enumerated in packages/worker/wrangler.jsonc run_worker_first (lines 109-110), while an SPA client route like `/join/<token>` deliberately takes no entry — the natural implementation requires zero wrangler changes, so the CLAUDE.md rule invocation carries no weight here.

(3) "Delegation of allowlist authority / open registration by proxy" — the invariant the finding defends no longer exists in that form, and the direction is a decided steer. multi-tenancy's "curated allowlist" was already amended to admit bounded self-service creation ("A group invite code admits a new bounded identity" scenario; self-service tenants are "placed on the same operator allowlist"). Member-minted invites are the decided product mechanism: S1/S3 ("the social graph cushions cold start... everyone they invite starts warm") and pages/08's functional requirements put the invite-link popover in members' hands as the UX contract. Bounding the delegation is structurally guaranteed by the OpenSpec workflow: the building change must delta self-service-signup's existing cap/expiry/limiter SHALLs to fork it, so "unbounded" minting can't slip through silently. Household-invite governance (who may mint) rides the same open governance question pages/08 q1 already carries, and D9 already provides the deployment-config axis if a switch is wanted — proposal-time, not pre-proposal.

Nothing here blocks or misdirects proposal drafting; the useful content of the proposed_resolution (invite link = cap-1 group code reusing the atomic claim-and-spend + limiter + no-oracle rejection) is a good default the building change will land on from the existing specs anyway.

### Widget micro-interactions need fine-grained callable tools that the coarse-tools doctrine forbids exposing to the model — adopt MCP Apps app-only tool visibility as a new tool-contract tier now
**Verdict: immaterial**

The mechanism cited is real (verified against ext-apps specification/2026-01-26/apps.mdx: visibility defaults ["model","app"]; "Host MUST NOT include tools in the agent's tool list when their visibility does not include \"model\""; hosts MUST reject app calls to non-app tools), and the spec set doesn't mention it. But it is not material pre-proposal: story 06 §4 Q1 already mandates a spike against the current MCP Apps SDK before the first widget change ("which protocol surface... what does Claude.ai support today"), and tool visibility sits in the same spec section as the view→tool-call machinery that spike must read — under the repo's planning doctrine the spike runs during proposal drafting, exactly when this gets settled. The finding's premise is also overstated: most cited micro-interactions map to tools that already exist and are already model-visible (update_grocery_list, add/remove_from_grocery_list, mark_pantry_verified — pages/05 §1 names it; brand-pick rides the existing checkpoint machinery per pages/05 §3), and the coarse-tools invariant forbids raw building blocks that bypass cache/validation/matching, not granular tools — a pipeline-wrapped broader/manual search doesn't violate it even if model-visible (the finding's own resolution concedes the wrapping). The "wrongly conclude unimplementable" horn is a false dichotomy: the living meal-plan-widget spec already establishes widget-initiated tool re-invocation from the iframe with graceful degradation. Finally, adopting an app-only tool-contract tier NOW, pre-spike, would commit to an unverified host capability (whether Claude.ai enforces visibility today is the spike's empirical question) — inverting the repo's evidence-first planning. If the proposal session wants a breadcrumb, the refined resolution below is the right one-clause enrichment of story 06 §4 Q1, not a new §3 commitment.

### The dual-use widget convergence has no band — band 2 would rebuild pages standalone and force a second rewrite
**Verdict: already_covered**

The finding's repo facts are accurate (two propose implementations today; no grocery/order MCP Apps; sendMessage commit at packages/widgets/src/ProposeCard.tsx:231-239; the no-frontier-turn requirement in openspec/specs/meal-plan-widget/spec.md). But its central claim — "no change owns the convergence architecture" — misreads what the spec set already provides, and the rest restates decided steers or logged open questions.

(1) Story 06 IS the convergence-architecture spec, not a missing one. §1 fixes exactly what the finding says is unowned: one implementation per widget in packages/ui-or-widgets, with per-host data plumbing defined for both hosts (props/TanStack Query + registered mutations vs tool-result hydration + host bridge, online-only). §3 fixes the Worker resource/tool wiring, self-containment, versioning, and per-host test-adapter consequences. §2 fixes the context-update contract as a design rule every proposal must satisfy ("for every widget interaction, specify (1) the backend write, (2) the context update, (3) source of truth on conflict"). This is D4/S4, already decided.

(2) The "band 2 rebuilds pages standalone" scenario requires implementers to violate their own page specs. pages/04 §2 makes the MCP App host a functional requirement of the plan-your-week slice ("MCP App host (story 06): propose runs inside Claude conversations via propose_meal_plan; every swap/pin/commit sends agent context updates; commit is the consolidated handoff"), and pages/05 binds story 06 to both grocery widgets. The band ordering already sequences correctly: the first widget-bearing slice is plan-your-week (band 2, named "Plan-your-week widget alignment" — the landing slot for the shared-layer extraction), and grocery/order (band 3) land after and consume it. Recipe Card's MCP widget already exists. No band-2 page other than plan-your-week is widget-bearing, so "rebuilding each one later" has no referent. Whether the shared layer is its own change or the first part of the plan-your-week change is exactly the slicing the overview explicitly delegates ("Dependencies, not a mandate. Each numbered band can be one or more OpenSpec changes") — a proposal-drafting call, not a spec-set defect.

(3) The spike clause of the proposed resolution restates existing text: story 06 q1 already mandates the MCP Apps SDK spike "before the first widget change", and CLAUDE.md's planning doctrine already forbids spike tasks and runs spikes during planning.

(4) The "latent spec conflict" is overstated. D4 constrains *mutating* interactions; widget iteration replays a stateless, read-only propose op (nothing written until commit), so the meal-plan-widget "no additional frontier-model turn" requirement does not collide with D4's letter. The genuine residual question — context-update granularity — is logged as story 06 q2 with a concrete recommendation (micro-writes silent, consolidated update at commit/send/close boundaries), and the README defines open questions as resolvable "before or during proposal drafting". When the contract is fixed, updating meal-plan-widget/recipe-card-widget specs in the same change follows the doctrine D8 already establishes for spec-contradicting deltas ("the band that lands the surface updates that spec in the same change — deltas, not silent drift").

(5) The sendMessage-vs-callServerTool canonical-pattern question is precisely what story 06 §2's per-interaction design rule instructs each widget proposal to answer.

Residue worth a one-line editorial touch (not a blocker, no new slice needed): 00-overview band 2 could gloss "Plan-your-week widget alignment" as "carries the shared widget-host layer (story 06) that band 3's grocery/order slices consume, plus the meal-plan-widget spec delta." That is a clarification of already-implied sequencing, safely made when the first band-2 proposal is drafted.

### The D9 deployment-profile flag has no home in the sequencing, and the 'Popular with Friends' band-2→band-5 implementation swap is implicit
**Verdict: already_covered**

The finding's facts are accurate (bands never name the profile flag; the badge appears in band 2, the lens in band 5; the trending read with min-signal guard exists per docs/ARCHITECTURE.md line 107), but its core claim — that a band-2 proposal must block or invent a bridge — is false: the bridge is already stated in composable pieces. (1) 00-overview.md "What already exists" maps the Recommended-panel reason rows onto the existing new-for-me/trending/picked-for-you signals, i.e. band 2's "Popular with Friends" is backed by the existing trending read. (2) DECISIONS.md D9 states the badge "reads the friend lens in both profiles (under self-hosted it equals deployment-wide trending, so no relabel is needed)" and that pre-profile deployments reduce exactly to today's shared-corpus experience with no data surgery — so shipping the badge over trending pre-band-5 IS the decided semantics. (3) pages/01-cookbook.md §2 says the same: "one implementation, both profiles", and tags the cold-start onboarding "story 01 … SaaS profile only per D9", steering it out of band 2. The profile flag's home is derivable, not missing: band 5 = story 01, whose decided model (§1) explicitly includes "Deployment profiles (D9)", and every D9-gated surface (People rendering, empty-corpus-on-join, curated set, cookbook onboarding) is a band-5/story-01 item; nothing earlier needs the flag. Whether band 5 opens with a dedicated deployment-profiles slice is intra-band change decomposition, which 00-overview explicitly delegates ("Dependencies, not a mandate. Each numbered band can be one or more OpenSpec changes") — a proposal-time concern. The resolution's curated-set-mechanics rider (operator-flags-via-admin) is scope creep: story 01 §5.5 deliberately tracks curated-set maintenance as an open question for proposal drafting, and resolving it is an operator product call — the one genuinely needs_user item, already tracked as open. Residual value of the finding (a one-line band-2/band-5 sequencing note in 00-overview) is a drafting nicety fine to land when band 2's cookbook proposal is written, per the D8 pattern of the landing band updating specs in the same change.

### The 'Leftovers' composer source contradicts a spec'd negative guarantee and blocks band 2's retrospective slice — decide now
**Verdict: immaterial**

Factually accurate but not a decide-now item. (1) The conflict IS the spec set's own documented question: pages/07 §2 ("Semantic conflict to resolve") states both admissible options — new `leftovers` type with eating-log semantics excluded from cook-count aggregates, or drop the source — and explicitly forbids the feared failure mode ("Don't silently overload `ready_to_eat`"); open question 1 flags it as "the one real product decision on the log." Per README doctrine, open questions are resolved before or during proposal drafting — that is the designated decision point, so nothing pre-proposal is blocked. (2) "Blocks band 2" is overstated: 00-overview's bands are "dependencies, not a mandate," and band 2's scope is "Retrospective shell with the existing cooking log tab" — whether the reworked composer even ships in that slice is itself proposal scoping. (3) The finding's exclusion cascade is mostly mechanical, not decisional: every living-spec aggregate enumerates included types (cadence counts recipe+ad_hoc only; last_cooked is MAX(date) WHERE type=recipe; group-insights counts type IN ('recipe','ad_hoc'); vibe satisfaction writes inside the log_cooked cook transaction), so an additive type is auto-excluded everywhere; the real spec delta is just the enum + narrowing the negative guarantee, exactly as the page sketches. (4) The one genuinely open sub-decision it pre-decides — story 03 q3's cost-per-meal denominator — belongs to band 4's analyzer proposal, and the proposed value is contestable: excluding leftovers rows denies batch cooking any cost-per-meal credit, and the mock's "~N meals cooked at home" label doesn't force either reading; deciding it before the numerator design exists risks ratifying the wrong number. (5) No operator round-trip is saved: the finding itself derives the answer from D5+D8+the page constraint (needs_user false), and that derivation is equally available to the drafting session. Direction for the drafter (not a new decision): keep the source, add `type=leftovers` (name-only, inline dims optional), narrow the cooking-history negative guarantee to "a leftovers row is an eating record, never a second cook," carry the cooking-history spec delta in the change that ships the composer, and leave the cost-per-meal denominator to the band-4 analyzer change per story 03 q3.

### Two open producer decisions should resolve to the no-cron option now, to keep bands 2-4 off the scheduled() serial surface
**Verdict: immaterial**

Facts check out (the cron spine at packages/worker/src/index.ts:211-299 is real; the CLAUDE.md serial rule is quoted accurately; neither question is answered by steers, DECISIONS.md, or openspec/specs), but the finding is a pre-answer to two questions the spec set explicitly schedules for proposal time: story 03 §2 says 'decide in proposal' and pages/02 §3 says 'a proposal decision', and README.md defines open questions as needing 'a product decision before or during proposal drafting'. Nothing pre-proposal is blocked by deferral. The sequencing-cost premise is overstated on three counts: (1) CLAUDE.md's serial rule is an implementation-time scheduling constraint the staggered-pipeline working mode already absorbs ('a finished plan makes the serial hop cheap'); (2) the cron options would not necessarily touch scheduled() wiring — body annotations 'via the facet-derivation cron' would extend the existing runFacetJob classify pass internals, and avoidability-in-cron would ride an existing phase, so the serialization trigger is much narrower than 'bands 2-4'; (3) the 'no user-visible gain' claim is wrong for annotations: render-time-parsing-only means guided cooking's ingredient tooltips/timers/mise-en-place light up only on hand-annotated recipes and every discovery-sweep import degrades to plain steps forever, whereas a cron annotator (fuzzy LLM work captured once, identity-keyed) is squarely capture-retrieve-narrow + S2's memoize-everything doctrine and gives the full experience corpus-wide — a genuine product tradeoff that should not be foreclosed now by an engineering-convenience argument. The avoidability half is near-certainly where its proposal lands anyway (the story already leans to the table; §3 mandates plain-code reads; capture stays reason-only under either option, so even the finding admits later upgrade is a read-path change) — deciding it early buys nothing. The useful residue is a one-line planning note, not a spec decision.

### Member identity does not exist at the token/session level, and member-vs-household composition rules for planning inputs are unspecified
**Verdict: immaterial**

The finding bundles three sub-claims; two are already covered and the third is real but resolvable during band-5 proposal drafting, not a blocker now. (a) Token-level member identity: factually right that neither the MCP grant (multi-tenancy: token "resolves to exactly one tenant") nor the /api session record (member-session-auth: "the tenant id plus created/refreshed timestamps") carries a member id — but story 01 §5 q6 explicitly tracks "verify the tenant/member split at token level" as an open question, §4 already decides the data half ("member-scoped tables gain a member key within the tenant"), and pages/13 already requires member-keyed auth in substance (per-member passkeys, grant metadata at issuance, sessions that "enumerate-by-member"; pages/14 notes invite codes go per-member). Per product-specs/README.md, open questions are resolved "before or during proposal drafting"; auth is untouched before band 5. (b) Composition: the vibes/cadence/budget scoping the finding calls contradictory is answered by story 01 §1's default rule — everything per-tenant today is household-shared unless in the explicit member-level split list; vibes are not in it, so they are household-scoped. Pages/10's Profile placement implies nothing (the same Profile shell houses the household store card, Kroger link, budget, per-meal cadence). The surviving residue — whose diet gate/rejects/favorites apply when member-scoped inputs feed household plan generation — is genuinely unwritten, but it is not material pre-proposal: band 1's propose work stays tenant-scoped either way, the schema unwind is exactly §4's already-decided member-key split, and band 5's implementation forcibly confronts composition the moment rejects/diet become member-keyed, with near-forced answers (diet union for safety, rejects union for volunteered pools, favorites = acting member). (c) Handle/tenant-id collision: already resolved in the direction the finding proposes — story 01 §3 makes @handle a "mutable display key over a stable member id (and stable tenantId)" (no key derivation from handles), pages/13 q2 lists the rename ripple and old-handle reservation, and pages/14 already proposes the opaque-ref fix for the ?tenant= leak. Cheap hygiene fix worth taking: add one explicit open-question line to story 01 §5 for member→household input composition in plan generation (q6 frames member-awareness as authorship, not gate composition), so band-5 planning cannot overlook it.

### Per-meal propose must be ONE op invocation with per-meal counts, not three independent sessions — otherwise cross-meal diversity, weather quotas, and determinism break
**Verdict: already_covered**

The spec set already pins the single-invocation, per-meal-counts contract the finding demands; the finding quotes the "independent per-meal sessions" phrase without engaging the contract-change section that resolves it.

1. One op with a per-meal counts map — already specified. stories/02-meal-dimension.md §2 ("Contract changes implied") lists "`propose_meal_plan` per-meal counts" as the tool change, in the same list that defines the cadence shape `{breakfast: 0-7, lunch: 0-7, dinner: 0-7}` two bullets up — the tool gains a counts map, one signature, not three calls. And docs/TOOLS.md already establishes the one-contract fan-out the finding asks for: `POST /api/propose` "runs the same shared operation with the same input and result shape (one contract)" and `display_meal_plan` "takes the same input... same shared stateless planner (`runProposeMealPlan`)" — so a params change to the shared op automatically covers all three surfaces.

2. "Sessions" are already client presentation, exactly as the finding's resolution proposes. pages/04 §2 "Session model" defines ONE session object — shared `{seed, variety, proteinWants, freeform}` + per-meal `{locked, overrides, excluded, facet pins, vibe override, sides}` sub-state — "persisted (localStorage today; keep)", with "per-meal sections are collapsible". Open q6 confirms it: "Session-shape migration (`v:3` per-meal) from existing single-nights sessions" — singular session migrating to a per-meal shape. One client session object with per-meal sub-maps replays as one request; it cannot denote three server sessions. The finding's own refinement ("collapsible sections over one session object, v:3 migration per pages/04 q6") restates pages/04 verbatim. The word "independent" in stories/02 §1 / pages/04 §1 scopes the per-meal client state (locks/pins/exclusions/sides per meal), not separate invocations; the summary bar's week-spanning variety stats (pages/04 §2) only compute over one result set, reinforcing the single-pass reading.

3. The residual semantics the finding wants decided are either forced by the above or already logged as open questions for proposal drafting (which README.md says is exactly when open questions get resolved): (a) whether weather-bucket quotas span breakfasts/lunches at all is explicitly open — stories/02 q4 ("Weather-bucket allocation currently spans 'nights' — does it apply to breakfasts and lunches too... or dinner-only?") — so the finding's "run ONE weather pass across all slots" would prematurely close a deliberately open product question; (b) the empty-meal fallback is already tracked three times (pages/04 q2, pages/10 q3, stories/02 §3 "define behavior when a meal has slots but no vibes of that meal", noting today's nudge-to-vibes-tab behavior) — duplicating it here adds nothing.

4. Minor overstatement: three same-seed calls would each be individually deterministic/replayable — the real hazard is cross-call coordination (diversify state, use-it-up decrement), and that hazard is already averted by the single-signature contract in stories/02 §2.

Optional non-blocking polish if the maintainer wants belt-and-suspenders: append one clause to stories/02 §1 propose bullet, e.g. "per-meal proposal sessions... over a shared seed and dials **(one engine invocation; sessions are client presentation)**". Not required for the proposal to land correctly.

### Meal-plan row identity, log_cooked auto-clear, and commit idempotence all key on recipe slug alone — the meal dimension breaks each; the spec set only names the log dedupe rule
**Verdict: wrong**

The finding quotes the current contracts correctly (docs/TOOLS.md:1013-1014 add-upsert/remove by slug, :992 auto-clear by slug; member-app-propose spec :65 commit upsert by slug; cooking-history spec :56 same-transaction plan-clear), but its central premise — that a recipe legitimately holds both a lunch row and a dinner row — is contradicted by the spec set itself. product-specs/pages/03-meal-plan.md §2 specifies recipe-unique plan rows as functional requirements: the per-meal add combobox "re-tags an existing row's meal if the recipe is already planned" and a grid pick of an already-planned recipe "moves" it (sides preserved). Page 03 explicitly flags actual mock bugs in the same section (occupied-slot silent delete, beyond-horizon hiding) and does not flag re-tag/move, so per the README's provenance rule these are spec'd intent: meal is a mutable attribute of the slug-keyed row, not part of its identity. Under that model, slug-keyed add-upsert, auto-clear, and commit idempotence all remain unambiguous — the "silently merges or clears the wrong one" failure state cannot arise. The claim that "the spec set only names the log dedupe rule" is also inaccurate: story 02 §2 mandates the same-pass TOOLS.md update to update_meal_plan/read_meal_plan rows and the log_cooked meal param; pages/03 §2 defines the full row model {recipe, planned_for?, sides[], meal, from_vibe?}; and pages/04 open question 4 explicitly captures the commit-duplicate decision ("give feedback or update the existing row (decide)") for resolution during proposal drafting — the finding's own resolution cites it. The proposed composite PK (tenant, recipe, meal) with (recipe, meal)-addressed ops and ambiguity-fallback clearing would contradict pages/03's spec'd behavior. Residual real work is mechanical proposal-stage wording (the upsert/auto-clear/commit sentences gain "and sets/updates the row's meal", matching re-tag) plus resolving pages/04 q4 — already anchored by story 02 §2's coordinated-pass mandate; no operator decision needed.

### The vibes tab's inline add-suggestions depend on a scheduled producer that does not exist — today add_vibe proposals are only produced on demand by suggest_night_vibes
**Verdict: wrong**

The claimed missing producer exists at every layer of the repo. openspec/specs/night-vibe-archetype-derivation/spec.md ("On-demand and scheduled derivation, bounded") mandates a scheduled generative reconcile pass — the pluggable `edge` producer of profile-reconciliation — that enqueues add_vibe proposals into pending_proposals under a per-run cap and records job health; a scenario references "successive daily runs". profile-reconciliation's "Pluggable synthesis" requirement independently requires the autonomous routine edge pass. docs/ARCHITECTURE.md documents it as the self-gated (~daily) archetype-derive pass, phase 5 of scheduled(), sharing one runDerivation core with the tool and the app trigger. The code ships it: packages/worker/src/index.ts runs runArchetypeDerivationJob(env) (night-vibe-suggest.ts) in phase 5. The member-app-core health-gating requirement the finding cites is itself evidence of the cron ("~20 hours, the cron's own constant"). So page 10's "the cron carries the load" (and D8's "the cron carries suggestion generation") is grounded — footer add-suggestion cards populate for app-first members today with no agent contact. The finding was misled by docs/TOOLS.md's reconcile preamble, which under-enumerates the queue's producers (signal cron + operator only) — that is a real but minor doc-lockstep nit for the vibes band, not a product-spec gap. The residual true kernel — derivation isn't meal-aware, so per-meal footer placement/"most mornings" rationale needs the cluster's meal classified — is a pre-proposal-immaterial drafting detail: stories/02 §2 already mandates the coordinated meal-field pass (vibes + log rows + tools), and classifying meal in the same naming generation mirrors the existing weather-bucket pattern; the example microcopy is mock painted-door per D5. No operator decision needed.

---

## 2. Minor findings (32) — resolution gists

All 32 minors were accepted and folded into the spec set: either directly into DECISIONS.md entries (noted), or as spec-text patches to the cited pages/stories/00-overview (the four reference maps were attached to 00-overview's sequencing section). Full original text: `tasks/minor-findings.md`.

### Consistency

1. **Instacart retailer targeting.** Superseded by the required feasibility spike: `products_link` cannot target a retailer, so yamp stores no Instacart retailer preference or override. The member chooses a retailer on Instacart Marketplace after handoff (story 04 §3).
2. **Sidebar badge definitions unowned.** No spec defined the grocery count; plan was half-defined. Resolved in the overview: grocery = derived to-buy count minus checked/in-flight; plan = meal rows only (projects excluded); people = pending inbound requests. Spec patch.
3. **Recipe Card 'kitchen location' vocab disagrees with pantry locations.** Widget said Produce/Spice drawer; page 06 defines six locations. Resolved: one vocab (page 06's), widget groups by actual pantry-row location else a deterministic category→default-location map. Spec patch.
4. **Propose session shape still carries cut controls' fields.** Resolved: the cuts are member-UI-only — the shared op keeps variety/protein-wants/freeform for the agent; the member session drops them; member-app-propose deltas per D8/D20. Spec patch (page 04 §2).

### Doctrine

5. **Feed health persistence.** Health is a capture rider on the existing sweep rotation + probe: last_polled_at/last_ok_at/last_status columns; 'Checking…' is client-only, never stored. Spec patch.
6. **Waste avoidability.** Decided: a deterministic versioned reason(+item-class)→avoidability table applied at analyzer read time — no LLM cron, events stay raw and retroactively re-classifiable. Spec patch (story 03 §2).
7. **Recipe body annotations producer.** Derived on the existing body_hash-gated facet-classify cron into a derived table; deterministic client parser as fallback; never render-time LLM. Spec patch; feeds D32's annotation contract.

### Datamodel

8. **'overlay' naming collision.** "Visibility lens" adopted exclusively for recipe visibility; "overlay" reserved for the shipped favorites/rejects table. Folded into D12 (terminology clause).
9. **pages/03 marks `from_vibe` as new.** It shipped in migration 0026 and propose commit already writes it; page corrected, delta table fixed. Spec patch.
10. **Pantry category→location migration.** Old location-flavored category values copy 1:1 to `location`; `category` goes NULL and is backfilled by a pipeline pass (capture→retrieve), never a hand mapping; dual-vocab reads during convergence. Spec patch (page 06 q4).
11. **Grant/session metadata storage.** The KV OAuth provider stays untouched; sibling advisory D1 metadata tables (mcp_grants_meta, web_sessions_meta) serve the Connections card; KV remains the auth authority. Spec patch (pages/13); consumed by band 7a per D25.

### Migration

12. **'Just Added' must not be redefined by visibility events.** new-for-me stays discovery-attribution-based; friend-accept and curated landing never flood Just Added (friend recipes surface via Popular with Friends/provenance). Spec patch.
13. **Aisle-map pooling key.** Already exists: the shared `stores` registry slug + `layout` store notes — "community-mapped" is the existing shared-notes behavior with a member UI (per D6). Spec patch (story 04 q2).
14. **shared-corpus's private-recipe escape hatch is vestigial GitHub-era text.** The band-5 lens proposal rewrites shared-corpus wholesale; zero private recipes exist to migrate; multi-tenancy's GitHub-era requirement filed for the same band's cleanup. Spec patch.

### Privacy

15. **Requester-supplied display names/notes are a phishing channel.** @handle always rendered (never replaced by a display name); notes inert length-capped plain text (carried into D24); nickname seeding shown editable at accept; two nickname hygiene rules added. Spec patch + D24.
16. **MCP-host widgets must carry no credential.** Widget payloads are data-only — never a token, session id, or signed URL; all MCP-host mutations are bridge tool calls under the connected member's grant. Folded into D15/D18's channel template.
17. **Disconnect-all semantics + metadata minimization.** Disconnect-all revokes all MCP grants only (sessions have their own control); device label from UA, coarse city geo at issuance, never raw IP; purged with the record. Spec patch (pages/13 q4/q6).
18. **Passkey removal ceremony vs lost devices.** The two lost-only-device flows named (recovery-email path; operator rotate()); lifecycle notifications on add AND remove; UV assertion required; honest threat model documented. Spec patch.
19. **Memoization boundary.** Identity-keyed shared memoization covers source-derived artifacts only; tenant-behavioral data (prices paid, follows, cook activity, waste) stays owner-scoped. Folded into D2 (amended text).
20. **D9 profile flag home + flip semantics.** Operator-owned deployed var read at request time; implicit edges computed by the lens, never materialized; SaaS→self-hosted flip refused above one non-empty household. Spec patch.

### Widgets

21. **Widget payload versioning must be bidirectional.** Every payload gains contract_version with a supported floor/ceiling and read-only degrade on unknown-newer; retroactive to ProposeCardData/RecipeCardData. Folded into D19.
22. **Story 06 q3/q4 already decidable from the protocol.** MCP-host I/O rides the host bridge over the grant (the iframe has no cookies); the shared component is plumbing-agnostic, member host owns class (b) queueing, MCP host online-only. Folded into D15/D18; questions closed.
23. **D4/README overstate RecipeRow as an MCP App.** RecipeRow is a shared @yamp/ui primitive, not a standalone widget. Folded into D4 (amended wording: four dual-use widgets + RecipeRow as primitive).
24. **Page 04 duplicate-at-commit already answered.** member-app-propose decides it (slug-keyed idempotent upsert + member-visible feedback); extended to per-slot identity semantics by D26-final's commit rule. Question closed by citation.

### Sequencing

25. **Momentum bank.** Zero/near-zero-backend re-skins (connect-modal, cookbook-unified-browse, recipe-detail-tweaks; profile Taste relayout) can ship while band 1 is in flight — pipeline-start candidates that exercise the app-ui gate without touching band-1 surfaces. Sequencing input alongside D25.
26. **Page 05 overstates 'household lines' as new.** grocery_list.kind and domain already exist with defined semantics; the mock's HOUSEHOLD group = kind='household'; the band-3 slice surfaces, not re-adds. Spec patch.

### Toolcontract

27. **update_feeds needs auto-follow semantics.** Under follow-gated relevance, agent-added feeds would be orphaned; update_feeds records the adding member as a follower (FEED-1 in the reference inventory). Spec patch + TOOLS.md same pass.
28. **Three pantry-staleness rules across surfaces.** One staleness-threshold table (per category class, verify-queue and worth-a-look tiers) in the shared pantry op, consumed by both pages and read_to_buy. Spec patch.
29. **run_worker_first + deploy-merge placement table.** Per-band routing placements enumerated. Instacart uses the existing `/api/*` path and introduces no callback or new entry; the D9 flag's config channel is checked against the merge allowlist. Attached to 00-overview.
30. **Reference inventory: complete MCP tool delta + new /api endpoint list.** The full enumerated tool/param/endpoint delta the spec set implies, so each band starts from the inventory instead of rediscovering it. Attached to 00-overview's sequencing.
31. **Per-band docs/spec lockstep map.** The same-pass TOOLS.md/SCHEMAS.md/ARCHITECTURE.md + openspec-delta checklist each band owes; a band PR missing a listed delta is incomplete. Attached to 00-overview.
32. **Persona/skills impact map.** The AGENT_INSTRUCTIONS.md edits each band owes (plugin republish rides the deploy); guarantees live in tool descriptions, choreography in skills; retired-preference-key greps before merge. Attached to 00-overview.
