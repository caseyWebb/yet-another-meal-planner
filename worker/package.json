{
  "name": "grocery-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker hosting the grocery-agent custom MCP server (read-only repo-data tools).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.15.0",
    "js-yaml": "^4.1.0",
    "smol-toml": "^1.3.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6",
    "wrangler": "^4.98.0"
  }
}
